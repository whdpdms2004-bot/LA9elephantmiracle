# 15. 버전 로그 — BSS 1000 목표

**측정 기준 단일화**: 2019~2023 학습 → **Val2024 전체 BSS**. 프로덕션 `submit_021`(836.503) 대비 ΔBSS.  
**분모**: `SE_row` = 행별 `(p_old−y)² − (p_new−y)²` 평균의 표준오차. 절대 σ는 차이 판정에 쓰지 않는다.  
**선택 규칙**: 가중치·하이퍼파라미터를 Val2024에서 고르지 않는다. fold 간 전이 실패를 3회 관측했다.

---

## 현재 위치

| | 값 |
|---|---:|
| **sj Public 최고 (`submit_030`)** | **964** ✅ |
| `submit_029` (훅 결함, 실효 w 0.684) | 963 |
| `submit_024` (성분 1축) | 945.0 |
| `submit_023` (offset만) | 916.70 |
| 팀 최고 (찬우) | 923.00 |
| sj Val2024 (`submit_030`) | 876.98 |
| **목표** | **1000** |
| 남은 거리 | **+36** |

**남은 +36을 무엇으로 채우는가**는 아래 「Public 결과」 절이 정한다 — 축을 늘린다.

### ★ 전이율이 예상을 크게 넘었다

```
submit_023 = 022 + logit offset            Public 916.70
submit_024 = 022 + 플래툰 성분결합 (offset 없음)  Public 945.0    (+28.3)
```

내부 ΔBSS는 **+13.75**였는데 Public은 **+28.3**이다. 전이율 **206%**.

가정했던 90%를 크게 상회한다. 두 가지 해석이 가능하다.

1. **2025 평가 데이터에서 플래툰 신호가 Val2024보다 강하다.** 좌우 스플릿은 시즌 무관한 투수 고유 특성이라 레짐 변화에 강할 수 있다
2. **offset과 플래툰이 겹친다.** 023은 offset으로 예측 평균을 낮췄는데, 성분 분해가 이미 레벨을 맞춰서 offset이 불필요했을 수 있다

3. **★ 2025 평가 데이터의 구성이 2024와 다르다.** 저물량·신인 투수 비중이 높으면 이득이 증폭된다

**3번을 뒷받침하는 실측이 있다.** D2(프로파일 유형 residual)의 `asof_pitcher_n` 구간별 분해:

| 투구수 구간 | 비중 | local ΔBSS |
|---|---:|---:|
| 1–99 | 3.09% | **+87.75** |
| 100–499 | 9.52% | +46.96 |
| 4000+ | 37.06% | +3.10 |

**저물량 구간에서 28배 크다.** 플래툰 스플릿도 같은 성격이다 — 표본이 두꺼운 투수는 `asof_pitcher_success_rate`가 이미 잘 맞추지만, 얇은 투수는 좌우 스플릿이 결정적이다.

### 이것이 다음 선택의 기준이 된다

> **저물량·신인 구간을 겨냥한 기법은 내부 검증이 과소평가한다.**  
> 반대로 고물량 구간을 다듬는 기법은 내부 검증이 과대평가할 수 있다.

내부 ΔBSS를 볼 때 **반드시 `asof_pitcher_n` 구간별로 분해**해서, 이득이 어느 구간에서 나오는지 확인한다. 저물량에 몰려 있으면 전이율을 높게 기대하고, 고물량에 몰려 있으면 낮게 기대한다.

**둘을 분리하려면 `submit_022`(offset 없는 기준선) Public이 필요하다.** 이것이 다음 제출 1순위인 이유다.

```
022 = X                (미측정)
023 = X + offset       = 916.70
024 = X + platoon      = 945.0
```

`X`를 알면 offset 효과와 플래툰 효과가 각각 확정되고, `025 = 024 + offset = 945 + offset효과` 여부가 정해진다.

### 즉시 도출되는 후보

**`submit_025 = 024 + 사전 등록 logit offset(−0.0276337141)`**

offset과 플래툰은 독립 축이다. offset은 이미 학습 데이터에서 결정된 상수이므로 다른 base에 적용하는 것은 리더보드 fitting이 아니다. 다만 023 대비 024가 크게 올랐다는 건 offset이 불필요했을 가능성도 있어, **022 Public을 먼저 봐야 방향이 정해진다.**

---

## V0 — 기준선 (2026-08-14)

| 항목 | 값 |
|---|---:|
| 프로덕션 `submit_021` Val2024 | 836.503 |
| 25종 enhanced 앙상블 | 777.675 |
| sj 성분 라인 `p_ie` (플래툰 없음) | 642.25 |
| 원본 47피처 XGB 기준선 | 687.18 |
| **Val2024 시드 σ_abs** | **6.24** |

### 확정된 방법론

| # | 내용 | 근거 |
|---|---|---|
| 1 | 노이즈 먼저 측정 | `021 vs 019 = +0.64 = 0.1σ` — 구분 불가였다 |
| 2 | 시즌 drift base_score | Val2023 −397 → −73.5, σ 56.4 → 23.0 |
| 3 | 셀 오프셋엔 전역 통제군 필수 | 없으면 어떤 축이든 +43 가짜 신호 |
| 4 | early stopping은 fold 간 전이 안 됨 | 2023 inner 10라운드 → 2024 base 683→195 |
| 5 | 분모는 `SE_row`·`σ_pair` | 절대 σ는 같은 base 공유 변형에 과도 |
| 6 | 최적 w는 fold 간 전이 안 됨 | 2022 0.5~0.6 vs 2024 0.2~0.3, 3회 관측 |

---

## V1 — 플래툰 × 성분 분해 (2026-08-14) → `submit_024`

**Val2024 836.503 → 850.255, ΔBSS +13.752, t_row 3.65**

```
P(success) = 1 − [p_middle + p_reverse − p_middle∧reverse + p_outside]
p_final    = 0.20·P(success) + 0.80·p_022        (game_type == R)
```

| 구성 | 값 |
|---|---|
| 플래툰 | `split(p,h) = EB(투수 p, 타자손 h) − EB(투수 p 전체)`, K=300, **정적 동결 테이블** |
| 성분 라벨 | `train.csv` asof 연속행 차분, 100.0000% 복원, 학습 전용 |
| 결합식 | 포함배제 (조건부 독립 깨짐, 잔차 상관 −0.0657) |
| 적용 범위 | R 행만 (F는 2023 레짐 붕괴로 동반 손상) |
| w | 0.20 고정, 검증에서 선택 안 함 |

**격리**: `no_platoon` +1.88(노이즈) → `platoon_K300` **+14.42**. 이득 전부가 플래툰.  
**재료**: `p_ie` 642.25 → **694.85**, 프로덕션 로짓 상관 0.8801 → **0.8508**.

6단계 검증 전 항목 통과. SHA256 `2275A1E0...`. 상세: [`submit/2026-08-14/SUBMISSION_LOG.md`](../submit/2026-08-14/SUBMISSION_LOG.md)

### 기각 (V1 라운드)

| 항목 | 판정 |
|---|---|
| 아웃카운트·경기중요도(LI) | 13개 축, LI 4종 두 fold 모두 net 음수 |
| 투수 개인 절편 | 180개 설정 중 2022 전부 음수 |
| 프로파일 유형 residual | 약한 base +16 → 강한 base에서 소멸 |
| 감쇠 외삽 (λ=0.33/0.5) | 격리 측정 효과 0, `mid_bias` 불변 |
| 성분 분해 단독 | 프로덕션 위 +1.88 (노이즈) |

---

## V2 — 플래툰 형태 탐색 (완료) → **현행 유지**

예나 커밋 `7ebfb3e` 반영. 두 가지가 드러났다.

1. **챔피언 피처는 `hand_success`(레벨, K=20)** 이고 sj 라인엔 split(차이)만 있다
2. 예나는 split을 **행 단위 as-of**로 만드는데 sj는 정적 테이블이다

**핵심 정리**: `test.csv`엔 라벨이 없어 추론 시 as-of를 계산할 수 없다. 검증/배포 행은 **반드시 동결 테이블** 값을 받는다. 쟁점은 **학습 행 featurize 방식** 하나다.

| 방식 | 학습 행 | 누수 | 배포 정합 |
|---|---|---|---|
| S static | 동결 테이블 | 자기 라벨 포함 | 형태 일치 |
| A row-asof | 직전까지 누적 | 없음 | 형태 불일치 |
| B season-asof | 이전 시즌만 | 없음 | **구조 일치** |

### 결과 — Val2024 ΔBSS (프로덕션 836.503 대비)

| arm | w=0.10 | w=0.15 | **w=0.20** | w=0.25 | direct_bss | corr |
|---|---:|---:|---:|---:|---:|---:|
| V0_none | 1.88 | 1.39 | −0.04 | −2.43 | 681.3 | 0.8801 |
| **V1_static_split** ← 현행 | 9.17 | 12.06 | **+13.81** | +14.42 | 705.7 | **0.8508** |
| V2_rowasof_split | 7.21 | 9.26 | +10.29 | 10.28 | 729.9 | 0.8659 |
| V3_seasonasof_split | 5.75 | 7.03 | +7.23 | 6.36 | 714.4 | 0.8693 |
| **V4_static_level** | −3.08 | −7.42 | **−13.62** | −21.69 | **187.5** | 0.7494 |
| V5_seasonasof_level | 2.24 | 1.90 | +0.59 | −1.69 | 688.1 | 0.8810 |
| V6_static_both | 0.72 | −1.56 | −5.60 | −11.39 | 286.4 | 0.7683 |
| V7_seasonasof_both | 6.03 | 7.42 | +7.72 | 6.93 | 708.8 | 0.8687 |
| V8_rowasof_both | 4.63 | 5.31 | +4.90 | 3.40 | 597.4 | 0.8584 |

### 결론 세 가지

1. **현행 `V1_static_split`이 9개 중 최적.** 변경 불필요. Public 945가 이를 뒷받침한다
2. **정적 레벨 피처는 파괴적이다** (`direct_bss` 705.7 → **187.5**). `EB(투수×타자손)`은 투수 성공률을 그대로 담아서 정적 테이블로 쓰면 자기 라벨이 통째로 새어든다. **차이(split)는 주효과가 상쇄돼 이 문제가 없다** — 팀통합 §1-1의 "주효과를 빼는 게 핵심"이 왜 필수인지가 수치로 증명됐다. 예나가 레벨을 as-of(K=20)로 만드는 이유도 이것이다
3. **as-of 학습이 오히려 나쁘다** (static +13.81 vs rowasof +10.29 vs seasonasof +7.23). 누수는 없지만 **배포 시 모든 행이 동결 테이블 값을 받는데 학습 때만 as-of를 쓰면 train/serve 불일치**가 생긴다. 이 문제에서는 정합성이 누수 회피보다 중요했다

> `direct_bss`가 높다고 결합 후가 좋은 게 아니다. V2는 direct 729.9로 가장 높지만 결합은 +10.29로 V1(705.7 / +13.81)보다 낮다. **상관이 결정적이다** — V1이 0.8508로 가장 독립적이다.

---

## V3 — 성분 모델 계열 확장 (2026-08-14)

고정 w=0.20, 프로덕션 836.503 대비. `ie_cat` +14.94 · `ie_CF_xc` +14.93 · `ie_xgb` +13.82(현행) · **`dir_CF_all` +7.08**.

> **같은 피처·같은 계열인데 성분 분해를 하면 +14.9, 안 하면 +7.1.** 분해가 이득의 절반 이상을 만들고 상관도 더 낮다(0.863 vs 0.884). 이후 라운드가 전부 이 축을 판 이유다.

LightGBM은 기여 없음. CatBoost 전환 이득 +1.12.

---

## V5 — sj × hw 2자 결합 (2026-08-14) → **기각**

```
sj 850.469   hw 639.664   로짓 상관 0.9075
w* = M⁻¹A  →  sj 1.1080,  hw −0.0700
```

**hw에 음수 가중치가 붙고 양수를 주면 전부 손해다.** 세 가지가 걸린다.

1. 상관 **0.908** — plan.md 가정(0.80~0.85)보다 훨씬 높다
2. `optimal_w` +4.678은 hw 기여가 아니라 sj 계수 1.108 즉 **sharpening**인데, 이미 LOMO −2.52로 기각된 항목이다
3. **`w*`를 Val2024에서 적합하고 Val2024에서 평가했다 — in-sample이고 t_row 1.74로 유의하지 않다**

> ⚠️ 3번이 팀 계획 전체에 걸린다. plan.md 절차대로면 같은 데이터로 가중치를 정하고 이득을 추정하게 되어 **1017~1056 추정이 in-sample 낙관일 수 있다.** 한 쌍으로 5자를 부정할 순 없지만 상관을 먼저 실측해야 한다. `val2024_pred.csv` 규격으로 sj/hw 두 라인을 올려뒀다.

부수 확인: hw v8의 anchor 격차는 **시드 축소가 아니라 하이퍼파라미터**가 원인(시드 5→20 곡선이 −12.52~−10.88로 평평).

---

## V6 — 성분 분해 심화 (2026-08-14) → **`submit_025`**

**Val2024 836.503 → 853.8, ΔBSS +17.31, t_row 4.76**

| arm | 단독 BSS | w=0.20 |
|---|---:|---:|
| `A4_shared_xgb` (024) | 694.85 | +13.81 |
| `B4_tuned_xgb` | 700.67 | +14.72 |
| `A4_shared_cat` | 740.31 | +15.04 |
| `C5_split_xgb` | 712.67 | +16.44 |
| **`E5_split_xc`** | **750.49** | **+17.31** |

**기여 분해: OUTSIDE 분할 +2.64 · CatBoost 계열 +1.23 · 성분별 튜닝 +0.91**

```
P(success) = 1 − [p_m + p_r − p_mr + p_ob + p_oz]
```

`OUTSIDE`(194,093)를 `ball=1`(159,871)과 `ball=0`(34,222)로 쪼갠 게 가장 컸다. 후자는 **존 안인데 실패** — 문제 정의에 없는 네 번째 모드다. 성격이 다른 둘을 한 모델이 맞추고 있었다.

성분 기저율이 10배 차이(`r` 0.225 vs `mr` 0.034)인데 파라미터를 공유하고 있었다. 희소할수록 얕고 강하게(leaves 18/12/8).

6단계 검증 전 항목 통과. 245,789행 26.9초(6 vCPU 환산 107초, 한도 18%). SHA256 `5D203AC6...`

---

## V8 — 성분별 플래툰 스플릿 (2026-08-14) → **기각**

가설의 **방향은 맞았다.** 성분별 좌우 스플릿 산포(K=300):

```
r 0.02039  >  ob 0.00991  >  m 0.00736  >  oz 0.00484  >  mr 0.00379
```

REVERSE의 좌우 의존이 MIDDLE의 **2.8배**다. 정의상 "포수 요구 반대"라는 성격과 일치한다.

**그런데 예측 이득이 없다.** 최고 `F3_comp_K600` +16.60 vs 기준선 +16.44 = **+0.16**(노이즈).

| arm | 피처 | 단독 BSS | corr | w=0.20 |
|---|---:|---:|---:|---:|
| `F0_global_only` | 97 | 712.67 | 0.8513 | +16.44 |
| `F3_comp_K600` | 107 | 709.92 | 0.8437 | +16.60 |

**이유**: REVERSE가 실패의 48%라 `control_success` 기준 전역 스플릿이 이미 REVERSE 신호에 지배된다. 성분별로 쪼개도 새 정보가 거의 없고 피처 10개 추가분만큼 과적합했다. 상관은 낮아졌으나(0.851→0.844) 단독 BSS도 같이 떨어져 상쇄됐다.

---

## ★ 9라운드에서 나온 구분선

| 되는 것 — **새 정보·새 구조** | Δ |
|---|---:|
| 플래툰 스플릿 | **+13.75** → Public 945 |
| OUTSIDE 분할 | +2.64 |
| CatBoost 계열 | +1.23 |
| 성분별 파라미터 | +0.91 |

| 안 되는 것 — **이미 있는 정보의 재표현** | Δ |
|---|---:|
| 성분별 플래툰 | +0.16 |
| 성분 모델 스태킹 | +0.37 |
| 감쇠 외삽 | 0 |
| 프로파일 유형 residual (강한 base) | 0 |
| 투수 개인 절편 | 2022 전부 음수 |
| 아웃카운트·LI | 전부 음수 |

**V8이 특히 명확하다.** 가설(REVERSE 좌우 의존이 MIDDLE의 2.8배)은 실측으로 맞았는데 이득이 0이었다. REVERSE가 실패의 48%라 전역 스플릿이 이미 그 신호에 지배되고 있었다.

> **다음 실험을 고를 때 이 기준을 먼저 통과시킨다.** "모델이 아직 못 보는 것인가, 이미 보는 걸 다르게 쓰는 것인가."

---

## V9 — 성분 모델 스태킹 (2026-08-14) → **기각**

**진단**: 프로덕션 836.5 / 내 성분 라인 750.5 / 결합 853.8. **86점 뒤진 파트너로 +17을 얻고 있다.** 파트너가 강해지면 가중치가 오르고 이득도 커진다.

성분 라인이 약한 건 **97피처만 쓰기 때문**이다. 파이프라인 재구축 대신 `enhanced_seed_oof_parts/`의 순방향 OOF를 성분 모델 피처로 넣는다.

대가는 학습 행이 2022~2023(492,997)으로 주는 것이라 격리해서 잰다.

| arm | 학습 행 | 단독 BSS | w=0.20 |
|---|---:|---:|---:|
| A_full_noStack (현행) | 1,221,585 | 712.67 | **+16.44** |
| B_2223_noStack | 492,997 | 627.86 | +12.06 |
| C_2223_stack | 492,997 | 638.09 | +12.42 |
| D_2223_stackMargin | 492,997 | 303.22 | +9.30 |

```
행 감소 손해  = B − A = −4.388
스태킹 순효과 = C − B = +0.366     ← 무시할 크기
```

프로덕션 예측을 성분 모델에 넣어도 거의 아무것도 안 준다. 성분 모델이 이미 같은 기저 피처를 갖고 있어서다.

**부산물로 학습 행 수의 가치가 정량화됐다 — 절반으로 줄이면 −4.39.** 앞으로 행을 버리는 설계는 이 비용을 넘어야 한다.

`D`는 단독 BSS가 303으로 붕괴했다. 성분 타깃에 성공 로짓을 부호 반전해 밀어넣은 구성이 틀렸다.

---

## V10 — 시드 확대 + R/F 가중치 (완료) → **`submit_026`**

| | 결과 |
|---|---|
| 시드 8 → 20 | **포화.** 8시드 대비 이득이 노이즈 이하 |
| F행 가중치 0 → 0.20 | **+4.07** (`f_bss` 528.8 → 563.4) |

F행(포스트시즌)을 학습에서 완전히 버리고 있었다. 가중치 0.20을 주면 R행 성능은 그대로인데 F행이 +34.6 오른다. 2025 평가 데이터에 F행이 있다면 그대로 전이된다.

시드는 8에서 멈춘다. 팀 표준 20~30은 **단일 모델**에 대한 것이고, 여기는 5성분 × 2계열 = 10모델을 이미 평균하고 있어 배깅 효과가 소진돼 있다.

→ `submit_026` = 025 + F행 가중치 0.20

---

## V12 — 타자 플래툰 (2026-08-15) → **`submit_027`**

투수 플래툰이 +13.75(Public 945)를 냈다. **대칭 축인 타자는 팀 전체가 비워뒀다.**

| arm | 피처 | 성분단독 | ΔBSS | t_row |
|---|---:|---:|---:|---:|
| G0 투수만 (현행) | 97 | 748.40 | +21.55 | 5.50 |
| G1 타자 플래툰 | 100 | 751.16 | +22.77 | 5.77 |
| G2 타자 **프로파일** | 103 | **626.50** | +13.26 | 2.85 |
| G3 둘 다 | 106 | 617.49 | +13.19 | 2.79 |
| **G4 타자 성분별 플래툰** | 105 | 747.47 | **+23.99** | **5.93** |

**V1에서 관측한 붕괴가 타자 축에서 그대로 재현됐다.** 정적 '프로파일'(레벨)을 넣으면 성분단독이 748 → 626으로 무너진다. 자기 라벨이 새기 때문이다. **주효과를 뺀 '스플릿'만 안전하다** — 이 규칙은 축과 무관하게 성립한다.

→ `submit_027`

---

## V14·V15 — 신경망 종단학습 (2026-08-15) → **기각**

성분 5개를 하나의 신경망에서 동시에 내고 합사건 확률로 종단 학습하는 구성. 사용자가 처음 제안한 아이디어 3의 원형이다.

| | Val2024 성분단독 |
|---|---:|
| GBDT 5모델 (현행) | **748** |
| MLP 종단 (small) | 331 |
| MLP 종단 (big, 3×512) | 402 |

**GBDT를 절반도 못 따라간다.** 표형 데이터 + 1.47M행 + 강한 범주형 상호작용이라는 조건에서 신경망이 불리한 전형적 사례다. 종단 학습이라는 **구조**가 틀린 게 아니라 **함수족**이 틀렸다. GBDT로 종단 학습을 하려면 5개 트리를 동시에 미분해야 하는데 부스팅 구조상 불가능하다.

---

## V16·V17 — 결합 가중치의 구조 (2026-08-15) → **w 유지**

V16(Val2024 단독)에서 최적 w가 **0.30~0.35**로 나왔다. 현행 0.20보다 크다. prob 공간과 logit 공간 차이는 0.05 미만으로 무의미하다.

**게이트 fold의 최적점을 따라가면 안 된다.** 세 fold를 전부 재보니:

| fold | base − 성분단독 격차 | w* |
|---|---:|---:|
| 2022 | **−3.0** | 0.50 |
| 2023 | **+1134** | 0.10 |
| 2024 | +89 | 0.35 |

> **w\*는 격차에 단조 감소한다.** 성분 라인이 base에 가까울수록 크게 섞고, 멀수록 적게 섞는다. 당연해 보이지만 실측으로 확인한 것은 처음이다.

2023은 base 자체가 −140으로 붕괴한 레짐 변화 연도다. 여기서 w=0.25는 −12.36을 낸다. **세 fold 모두 양수인 최대 w = 0.20** 을 채택한다.

---

## V18 — 극단 예측값 cap (2026-08-15) → **기각**

팀 T1(찬우)의 "정상 연도 −10, 레짐 변화 시 +800 방어" 주장을 검증했다. cap을 걸어도 2023의 붕괴가 회복되지 않는다. 2023 손실의 원인은 극단값이 아니라 **레벨 전체의 이동**이다. cap은 꼬리만 자르므로 원인을 건드리지 못한다.

---

## V19 — 팀 ID + 카운트별 플래툰 (2026-08-15)

| arm | 피처 | 성분단독 | ΔBSS | t_row |
|---|---:|---:|---:|---:|
| H0 현행 | 105 | 745.56 | +23.60 | 5.83 |
| H1 팀 ID 범주형 | 109 | 762.13 | +24.99 | 6.33 |
| **H2 카운트별 플래툰** | 108 | 761.86 | **+32.04** | **7.47** |
| H3 둘 다 | 112 | 778.38 | +33.13 | 7.91 |

### ★ 이 대회에서 가장 큰 발견

V8에서 **구조적으로 동일한** 피처가 +0.16으로 죽었다. 차이는 단 하나다.

```
V8   split = EB(투수 × 좌우) − 리그평균          → +0.16
V19  split = EB(투수 × 카운트) − 리그평균 − 전역카운트효과 → +8.44
```

**주효과를 몇 겹 빼느냐가 전부다.** 리그평균만 빼면 남는 신호의 대부분이 "카운트 자체의 효과"이고, 그건 이미 원본 피처(`balls`, `strikes`)에 들어 있어 중복이다. **전역 카운트 효과까지 빼야** 비로소 "이 투수가 이 카운트에서 남들보다 얼마나 다른가"라는 순수 상호작용만 남는다.

> **계층 차감(layered subtraction): 상호작용 피처는 관여하는 모든 주효과를 빼야 값이 있다.**

---

## V20 — 카운트 플래툰 세 fold 검증 (2026-08-15) → **`submit_028`**

| fold | w=0.20 | **w=0.25** | w=0.30 |
|---|---:|---:|---:|
| 2022 | +43.51 | **+50.92** | +56.94 |
| 2023 | +18.09 | **+3.68** | −18.30 |
| 2024 | +30.36 | **+36.27** | +38.99 |

카운트 플래툰이 2023을 +4.58 → **+18.09** 로 끌어올렸다. 세 fold 모두 양수인 최대 w가 0.20 → **0.25** 로 올라간다.

**w=0.30은 기각한다.** 2023이 음수로 돌아선다. 레짐 변화 확률을 ~17%로 잡으면 기대값이 음수다(−1.08).

→ `submit_028` (Val2024 +36.27)

---

## V22·V23 — 계층 차감의 일반화 (2026-08-15) → **`submit_029`**

V19의 원리를 다른 축으로 확장했다. 후보 5종 전부 계층 차감을 적용.

| arm | 성분단독 | ΔBSS |
|---|---:|---:|
| J0 현행 | 760.08 | +36.07 |
| **J_P_hi 투수 × 이닝** | 761.96 | **+39.49** |
| J_P_hb 투수 × 타순 | 755.32 | +38.45 |
| J_P_cnt 투수 × 카운트(추가) | 766.15 | +37.61 |
| J_B_cnt 타자 × 카운트 | 746.56 | +36.93 |
| J_all 전부 | 721.31 | +39.20 |

**전부 넣는 것(J_all)이 최선이 아니다.** 성분단독이 760 → 721로 무너진다. 축을 늘릴수록 각 셀의 표본이 얇아져 EB 수축으로도 못 막는다. 이닝 축 하나만 추가한다(+2.98).

세 fold 검증에서 w=0.25가 그대로 유지된다(2022 +52.23 / 2023 +3.38 / 2024 +39.25).

→ `submit_029` (Val2024 +39.25)

---

## V25 — 상류 reverse scale 재최적화 → **현행 유지**

성분 층이 하류에 붙었으니 상류 `reverse_matchup_scale`(0.475)이 과할 수 있다는 가설. `reverse20` 성분이 npz로 남아 있어 재학습 없이 임의 scale 을 재구성할 수 있다.

**첫 시도는 앵커를 틀렸다.** `p(s) = p019 + …` 로 썼는데 019는 reverse20 보정 자체가 없는 시스템이다. 스크립트에 넣어둔 재구성 검증(836.503 기대 → 835.861 산출)이 이걸 잡았다. 올바른 식은 `p(s) = p021 + (s−0.40) × 0.6085 × reverse20` 이고, 021/022/020 세 지점을 최대오차 5.9e-08로 재현한다.

> **재구성식에는 항상 기지의 지점 재현 검증을 붙인다.** 이번엔 그게 없었으면 잘못된 결론을 냈다.

결과: `w=0.25` 고정 시 scale 0.20~0.90 구간의 ΔBSS 폭이 5 미만으로 **평평하다.** 최적점이 없으므로 현행 0.475 유지.

---

## V26 — 합성식 보정 (2026-08-15)

```
P(success) = 1 − (c_m·p_m + c_r·p_r + c_mr·p_mr + c_ob·p_ob + c_oz·p_oz) − c0
```

계수 (1,1,−1,1,1)은 다섯 성분이 완벽히 보정돼 있다는 가정이다. 참 확률에는 맞지만 추정 확률에는 각자의 편향이 있다.

| arm | 성분단독 | ΔBSS | pred_mean |
|---|---:|---:|---:|
| K0 고정 (현행) | 765.37 | +39.36 | 0.48795 |
| K1 자유 계수 | 699.50 | **+28.38** | — |
| **K3 절편만** | **782.75** | **+40.73** | 0.48603 |

**K1(계수 6개 OOF 적합)은 −11점이다.** `c_mr`이 −1.00 → −2.54로 2.5배가 되는 과적합. 기각.

**K3(절편 1개)은 +1.37.** 편향 +0.185%p → −0.008%p. 팀 벌점 공식 `401,000 × 0.00185² = 1.37` 과 소수점까지 일치한다. `c0 = 0.0077`, 2022/2023 OOF 에서만 적합했고 2024는 쓰지 않았다.

---

## ★ V27 — 빌드 결함 발견: 훅 중복 주입

`submit_030` 빌드 중 `assert script.count(OLD) == 1` 이 실패했다. 원인을 파니 **`submit_027`·`028`·`029` ZIP이 검증한 구성을 구현하고 있지 않았다.**

각 빌드는 이전 ZIP의 `script.py`에 성분 블록을 주입하는 방식인데, 훅 지점으로 삼은 줄(`if len(prediction) != len(test) …`)이 주입 후에도 그대로 남는다. 다음 빌드가 **또** 그 앞에 끼워 넣는다.

```
submit_025   정의 1 / 훅 1    901줄   정상
submit_026   정의 1 / 훅 1    904줄   정상 (블록을 치환만 함)
submit_027   정의 2 / 훅 2   1037줄
submit_028   정의 3 / 훅 3   1183줄
submit_029   정의 4 / 훅 4   1342줄   ← 1326,1328,1330,1332행
```

Python은 마지막 정의를 쓰므로 **피처 구성은 맞다.** 문제는 훅이다. 혼합이 연쇄로 적용된다.

```
q_k = w·p_ie + (1−w)·q_{k−1}   ⇒   q_k = (1−(1−w)^k)·p_ie + (1−w)^k·b
w = 0.25  →  w_eff = 0.4375 / 0.5781 / 0.6836
```

### 실제로 낸 점수 (V29, Val2024)

| 패키지 | 훅 | w_eff | ΔBSS | t_row |
|---|---:|---:|---:|---:|
| submit_026 | 1 | 0.2500 | +39.07 | 7.14 |
| submit_027 | 2 | 0.4375 | **+43.27** | 4.52 |
| submit_028 | 3 | 0.5781 | +32.29 | 2.55 |
| submit_029 | 4 | 0.6836 | **+16.12** | 1.08 |
| **submit_030** | **1** | **0.2500** | **+40.48** | **7.61** |

**`submit_029`는 의도한 +40의 40%밖에 안 낸다.** 반대로 `submit_027`이 우연히 +43.27로 가장 높은데, 이건 w_eff 0.4375가 **2024 fold에서만** 좋기 때문이다(V17: 2023은 w 0.30에서 이미 −36). 채택 근거가 되지 못한다.

### 왜 6단계 검증이 못 잡았나

라벨이 없는 test 셋으로만 돌리므로 **BSS를 잴 수 없다.** 실행 성공·행 독립성·범위는 전부 통과한다. 예측 평균조차 0.4807 vs 0.4795로 차이가 미미하다.

### 재발 방지

빌드 스크립트에 구조 불변식을 넣는다.

```python
assert script.count("def component_features") == 1
assert script.count("def component_blend") == 1
assert script.count("prediction = component_blend") == 1
```

> **주입식 빌드는 "주입에 성공했는가"가 아니라 "주입 후 구조가 의도대로인가"를 확인해야 한다.** 성공 여부만 보면 중복이 조용히 쌓인다.

---

## V28 — 깨끗한 재빌드 → **`submit_030`**

깨끗한 `submit_026`(훅 1회)을 base로 삼고, `submit_029`의 최신 성분 블록(155줄)과 모델 81개·룩업 4종을 이식했다. **재학습 없음.** 025 세대 잔존 파일은 제거했다.

| | 값 |
|---|---|
| 구성 | 5성분 + 플래툰 4축(투수·타자·카운트·이닝) + F행 0.20 + w 0.25 + 절편 0.0077 |
| 정적 검사 | 컴파일 OK, 주입 블록 미정의 참조 0, c29 모델 81개, 잔존 c25 0 |
| 6단계 검증 | **전 항목 통과**, 추론 27.0초(6 vCPU 환산 108초 / 600초 한도) |
| 예측 | 평균 0.480702, sd 0.047928, 범위 [0.2978, 0.6494] |
| **Val2024** | **+40.48** (t_row 7.61) |

---

## ★★ Public 결과 — `submit_030` 964, `submit_029` 963

내부 지표와 Public의 관계가 확정됐다. 앞으로의 모든 선택이 여기서 갈린다.

| 제출 | 성분 축 | 실제 w | Val2024 | Public |
|---|---:|---:|---:|---:|
| `submit_023` | 없음 (offset만) | — | — | 916.70 |
| `submit_024` | **1축** (투수 플래툰) | 0.20 | +13.75 | 945.0 |
| `submit_029` | **4축** | 0.684 (결함) | +16.12 | **963** |
| `submit_030` | **4축** | 0.250 | +40.48 | **964** |

두 쌍을 따로 보면 답이 나온다.

```
029 vs 030   피처 동일, 가중치만 다름   내부 +24.36  ->  Public  +1
024 vs 030   축 1개 -> 4개              내부 축기여 +13.86  ->  Public +19
```

> ### Val2024는 '정보 추가'에는 예측력이 있고, '가중치 조정'에는 없다.
> 축을 늘린 +13.86은 Public +19로 나왔다(전이 137%). 가중치를 바꾼 +24.36은 Public +1이다(전이 4%).

**결론: 가중치는 그만 만진다. 축을 늘린다.** 이 판단은 리더보드로 상수를 재조정하는 것이 아니라(RULES §2), **어떤 종류의 실험에 자원을 쓸지**를 정하는 것이다. `w`는 여전히 세 fold 규칙으로만 정한다.

부수적으로, 결함이 있던 `submit_029`(실효 w 0.684)가 963을 냈다는 사실은 2025 환경에서 성분 라인이 base와 비슷한 수준임을 시사한다. 2022 fold(w\* 0.50)에 가까운 환경이다.

---

## V30 — 결합 가중치를 투구량 구간별로 (2026-08-15) → **세 fold 통과**

V17에서 `w*`는 격차(base − 성분단독)에 단조 감소함을 확인했다. 그런데 격차는 fold 안에서도 균일하지 않다. 구간별로 재보니 **부호까지 뒤집힌다.**

| 구간 | 2022 격차 | 2023 격차 | 2024 격차 | 평균 |
|---|---:|---:|---:|---:|
| 0–99 | −97.8 | **+5481.1** | +75.8 | +1819.7 |
| 100–499 | +184.7 | +3281.8 | +79.9 | +1182.1 |
| 500–1999 | +203.0 | +1457.8 | +129.5 | +596.8 |
| 2000–3999 | −70.7 | +1143.4 | +87.5 | +386.7 |
| **4000+** | +3.1 | **−204.3** | +81.0 | **−40.0** |

> **고물량 구간에서는 성분 라인이 프로덕션 base보다 낫다.** 그리고 2023 붕괴는 저물량 구간에 몰려 있다(0–99에서 격차 +5481). 개인화 EB 테이블이 표본이 얇을수록 레짐 변화에 뒤집힌다.

**D2 진단과 방향이 반대여서 헷갈리기 쉽다.** D2는 "성분 피처가 없을 때 대비 어디서 이득이 큰가"를 쟀고(저물량 +87.75 vs 고물량 +3.10), V30은 "성분 라인이 프로덕션 base를 어디서 이기는가"를 잰다. 다른 질문이다.

### 채택 — 규칙 그대로

구간별로 '세 fold 모두 양수인 최대 w'를 뽑으니 **4000+ 구간만 0.25 → 0.45**로 올라간다. 나머지 넷은 0.25 그대로다. 격차 순 단조 제약(W2)을 걸어도 같은 답이 나온다.

| arm | 2022 | 2023 | 2024 |
|---|---:|---:|---:|
| W0 전역 0.25 | +51.64 | +22.76 | +38.06 |
| **W1 구간별** | **+57.66** | **+60.94** | **+39.00** |
| W3 2024 최적 | +61.00 | **−24.16** | +42.00 |

**W1 − W0 = 2022 +6.02 / 2023 +38.19 / 2024 +0.94.** 세 fold 모두 양수다. W3(2024 최적)은 2023에서 −24로 무너지므로 기각 — 게이트 fold 최적점의 함정을 또 한 번 확인했다.

**다만 기대값은 크게 잡지 않는다.** Public 결과가 가중치 조정의 전이율을 4%로 못박았고, W1의 2024 이득은 +0.94뿐이다. 이건 점수 상승용이 아니라 **레짐 변화 보험**이다 — 2025가 2023 같은 해라면 +38을 지킨다. 비용이 0(재학습 없음, `asof_pitcher_n`은 입력 피처)이므로 싣는다.

---

## V32 — 2023 붕괴는 레벨인가 형태인가 (2026-08-15) → **형태다**

`w`를 못 올리는 유일한 이유는 2023이다(격차 +987). 그 정체를 오라클 레벨 보정으로 갈랐다.

| fold | 라인 | raw | prob 이동 | logit 이동 | 편향 %p | 레벨 기여 |
|---|---|---:|---:|---:|---:|---:|
| 2022 | base | 2296.92 | 2301.50 | 2301.71 | +0.338 | +4.80 |
| 2022 | 성분 | 2241.39 | 2251.83 | 2250.79 | −0.510 | +10.44 |
| 2023 | base | −140.20 | 60.68 | 60.19 | **+2.241** | **+200.88** |
| 2023 | 성분 | −1127.09 | −1034.14 | −1059.41 | +1.524 | +92.96 |
| 2024 | base | 836.50 | 836.53 | 836.53 | +0.024 | +0.03 |

```
2023 raw 격차          986.90
     레벨 보정 후 격차 1094.82
     레벨이 설명하는 몫 −107.92   (−10.9%)
```

**레벨을 오라클로 완벽히 맞춰도 격차가 오히려 벌어진다.** 성분 라인의 2023 레벨 편향(+1.524%p)은 base(+2.241%p)보다 오히려 작다. 문제는 순위·기울기다.

> **w 상한은 진짜다. 가중치로 풀 수 없고 성분 라인 자체를 고쳐야 한다.** 그리고 Public이 말해주듯 w를 푼다고 점수가 오르지도 않는다. 두 결과가 같은 방향을 가리킨다 — **축을 늘려라.**

덧붙여 2024 base의 레벨 편향이 0.024%p인 것은 `submit_021`이 시즌 offset을 이미 적용했기 때문이고, 2022/2023 base(enhanced 25종 평균)는 그게 없어 +0.34/+2.24%p다. **fold 간 base가 같은 시스템이 아니다.** 세 fold 규칙을 해석할 때 이 비대칭을 기억해야 한다.

---

## V33 — 계층 차감 축 6종 추가 (2026-08-15) → **말랐다**

사용자가 처음 지목했던 축(아웃카운트, 경기중요도)을 포함해 6종을 현행 4축 위에 하나씩 얹었다. 전부 계층 차감 `EB(투수, 타자손, 축) − EB(투수, 타자손)`.

| arm | 피처 | 성분단독 | ΔBSS | 현행 대비 |
|---|---:|---:|---:|---:|
| M0 현행 | 111 | **746.85** | +39.00 | +0.00 |
| M_base 주자상황 | 114 | 734.57 | +40.64 | +1.64 |
| M_outs 아웃카운트 | 114 | 733.71 | +38.30 | −0.70 |
| M_score 점수차 | 114 | 707.84 | +36.59 | −2.41 |
| M_li 경기중요도 | 114 | 721.30 | +36.28 | −2.72 |
| M_opp 상대팀 | 114 | 646.92 | +34.06 | −4.94 |
| M_month 시기 | 114 | 643.75 | +31.67 | −7.33 |

> **여섯 축 전부가 성분단독 BSS를 떨어뜨렸다** (746.85 → 707~735). 유일한 양수 `M_base`의 +1.64는 6개 중 2024 최고를 고른 값이라 선택 편향 안이다. V23의 `J_P_hi`는 같은 조건에서 +3.42였고 세 fold를 통과했다 — 이건 그 절반이다.

**투수 × 타자손 × 축 계열의 계층 차감은 네 축(손·카운트·이닝·타자)으로 끝났다.** 111피처에서 포화다. 새 축을 넣으면 기존 축의 신호를 희석할 뿐이다.

이 결과가 다음 방향을 정한다 — **축이 아니라 구조**(V35).

---

## V36 — 합성 라인의 형태 보정 (2026-08-15) → **전부 기각**

V32가 남긴 질문에 답한다. 2023 붕괴가 형태 문제라면 단조 재보정으로 잡히는가.

V26 K1은 **성분별 계수 6개**를 적합해 과적합했다. 여기서는 성분을 건드리지 않고 합성 결과 `p_ie` 하나에 단조 사상만 씌운다 — 파라미터 1~2개거나 단조 제약이 있다. 그리고 **정방향·역방향 세 조합 전부**에서 잰다. 사상이 fold 간에 안정하지 않으면 2024에서 좋아도 채택하지 않는다.

### Δ결합 BSS

| arm | 2022 | 2023 | 2024 | 최악 |
|---|---:|---:|---:|---:|
| identity | 0.00 | 0.00 | 0.00 | 0.00 |
| intercept | −1.92 | +2.97 | +2.52 | −1.92 |
| platt | −114.08 | +2.72 | −30.54 | −114.08 |
| isotonic | −121.31 | −86.10 | −17.37 | −121.31 |
| beta | −162.28 | +0.14 | −20.37 | −162.28 |

> **형태 보정은 fold 간 전이가 전혀 없다.** 2022에서 isotonic이 −121, beta가 −162다. 성분단독으로 보면 beta는 2241 → 269로 완전히 무너진다. 한 해에서 배운 확률 사상이 다른 해에 그대로 틀린다.

**부수 확인**: 절편(`submit_030`에 실려 있는 `c0=0.0077`)도 만능이 아니다. 2023+2024에서 적합해 2022에 쓰면 −1.92다. 다만 `submit_030`의 c0는 **가장 최근 두 fold(2022+2023)에서 적합해 미래로 외삽**하는 올바른 방향이고, 그 방향의 실측은 V26의 +1.37이다. 유지한다.

V32 + V34 + V36을 합치면 결론이 하나다.

> **성분 라인의 사후 보정은 끝났다.** 레벨(V26 절편)은 이미 먹었고, 형태(V34 수축, V36 단조 사상)는 전이가 없다. 개선은 라인 내부에서 나와야 한다.

---

## V34 — 찬우(cw)의 보정 계열 이식 (2026-08-15) → **기각, 그러나 성격이 밝혀졌다**

`cowork/cw/submit_v8_mlp.zip`의 `model/params.json`에서 구성을 읽었다.

```
target_rate 0.47353   logit_center_C0 −0.0496   logit_target_C1 −0.10598
logit_scale 0.85(A) / 1.0(B,C)   cap 0.15/0.20   floor 0.15  ceil 0.85

z' = (logit(p) − C0) × scale + C1 ;  p' = clip(sigmoid(z'), target±cap)
```

핵심은 `scale < 1`이다. 로짓 폭 전체를 base rate 쪽으로 수축시킨다. V18에서 기각한 '극단값 cap'은 꼬리만 자르지만 이건 모든 행을 비례 축소한다 — V32가 2023 붕괴를 형태 문제로 판정했으므로 별개 후보다.

(찬우는 결합 비율을 리더보드 점수로 정했다 — `blend_source: "LB-measured"`. 그 부분은 가져오지 않는다. RULES §2.)

### ΔBSS (cap 무제한, 성분 라인에 적용)

| scale | 2022 | 2023 | 2024 |
|---:|---:|---:|---:|
| 0.70 | **−40.50** | **+17.82** | −19.47 |
| 0.85 | −16.33 | +10.26 | −5.78 |
| 1.00 | −0.40 | −4.01 | +2.91 |
| 1.10 | +5.87 | −16.93 | +5.96 |

> **수축은 순수한 레짐 보험이다.** 2023(레짐 변화)에서 벌고 2022·2024(정상)에서 그만큼 잃는다. 세 fold 모두 양수인 조합이 하나도 없다 → 기각.

`cap`은 0.15·0.20·무제한이 전부 같은 값을 낸다. 예측이 `target ± 0.15` 밖으로 나가지 않는다. **V18의 cap 기각이 재확인됐다.**

찬우가 A계열에 0.85를 건 것은 합리적인 헤지다. 다만 sj 라인은 이미 성분 분해로 레짐 대응을 하고 있어 중복이고, 세 fold 규칙을 통과하지 못한다.

---

## V31 — sj × hw 2자 결합 재측정 (2026-08-15) → **기각, 그리고 팀 계획 수정 필요**

V5에서 한 번 기각했지만 그때 hw 예측은 구버전이었다. 이후 v7(Public 847.76)·v8이 나왔으므로 다시 쟀다. 공통 규약 `cowork/*/val2024_pred.csv`(2019~2023 학습 → 2024 예측).

| | Val2024 BSS | pred_mean |
|---|---:|---:|
| hw | **639.66** | 0.49649 |
| sj 제출 파일 | 850.47 | 0.48755 |
| sj base(`submit_021`) | 836.50 | 0.48635 |
| **sj 성분결합 라인** | **876.98** | 0.48605 |
| 실제 평균 | | 0.48610 |

**logit 상관 hw × sj030 = 0.9095.** 결합 곡선은 `w_hw = 0`이 최적이고, 0.1만 섞어도 −8.07이다. 모든 w > 0에서 단조 하락한다.

### 구간을 나눠도 마찬가지다

| 구간 | 비중 | hw | sj030 | 차이 |
|---|---:|---:|---:|---:|
| 0–99 | 3.12% | 429.6 | 577.0 | −147.4 |
| 100–499 | 9.52% | 372.0 | 636.8 | −264.8 |
| 500–1999 | 25.03% | 663.5 | 935.2 | −271.7 |
| 2000–3999 | 25.27% | 551.3 | 792.9 | −241.5 |
| 4000+ | 37.06% | 615.4 | 827.6 | −212.2 |
| R행 | 88.16% | 659.1 | 873.4 | −214.3 |
| **F행** | 11.84% | 167.3 | 578.1 | **−410.8** |

**모든 구간에서 진다.** 유일한 국소 이득은 0–99 구간(3.12%)에서 `w_hw=0.2`일 때 local +13인데, 비중을 곱하면 전역 기여가 노이즈 이하다.

### ★ plan.md의 팀 결합 기대치를 수정해야 한다

> plan.md Phase E는 5인 결합으로 **1017~1056**을 기대한다. 그 계산은 멤버들의 실력이 비슷하고 상관이 0.7 정도라고 가정한다. **실측은 실력차 237 BSS, 상관 0.91이다.** 이 조합에서 앙상블 이득은 0이다.

앙상블이 이득을 내려면 **비슷한 실력 + 낮은 상관**이 필요하다. 지금 필요한 것은 인원 수가 아니라 **찬우(Public 923)와 예나의 `val2024_pred.csv`** 다. 그 둘은 실력 격차가 작아 결합 여지가 있을 수 있다. hw는 이미 규약대로 올렸고 sj도 올렸다.

F행 −410.8은 별개로 hw에게 전달할 가치가 있다. hw 파이프라인이 포스트시즌 행을 사실상 버리고 있다는 뜻이고, sj는 V10에서 F행 가중치 0.20으로 +4.07을 얻었다.

---

## 제출 우선순위

| 순위 | 패키지 | 근거 |
|---|---|---|
| **1** | **`submit_030`** | 검증된 구성을 실제로 구현한 유일한 최신 패키지 |
| 2 | `submit_022` | offset 효과와 플래툰 효과를 분리하는 진단. 여전히 미측정 |
| — | `submit_027~029` | **제출하지 말 것.** 구현이 설계와 다르다 |

---

## 대기 중인 후보

| 출처 | 항목 | 근거 |
|---|---|---|
| 팀 T4 (예나) | 계열 단위 가중 앙상블 | `0.5×mean(LGB) + 0.5×mean(Cat)` → +151.4 |
| 팀 T5 (찬우) | 얕은 트리 `max_leaves=6` | 두 연도 모두 노이즈 초과, 단독 미검증 |
| 팀 T3 (예나) | isotonic | sj는 beta만 보유 |
| 팀 F2 (정희원) | 팀 ID 범주형 | V19 H1에서 +1.39 확인, 이닝 축과 택일 미검증 |
| sj | 계층 차감 축 추가 탐색 | V19/V22가 유일하게 반복 성공한 방향 |
| **팀 Phase E** | **5인 결합** | plan.md 1순위, 예상 **1017~1056** |

> **1000 도달 경로에서 팀 결합이 여전히 가장 크다.** 개인 라인은 Val2024 +40까지 왔지만 단독으로 +55를 더 만들기는 어렵다. 공통 Val2024 예측 CSV 공유가 필요하다 — `cowork/*/val2024_pred.csv` 규약을 `.gitignore` 예외로 이미 열어뒀고 hw는 올려뒀다.
