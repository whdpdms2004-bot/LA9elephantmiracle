# 다음 캠페인 계획 — 2026-08-28 이후

작성 2026-08-27 (Public 1080.425 직후). 마감 09-01.
근거는 `PROGRESS_workflow.md`(잔여 항목) · `SUBMISSIONS_AND_ENSEMBLE.md`(멤버 구조) ·
`results.csv`(실험 등록부).

---

## 0. 우선순위를 정하는 원리 — 전이율

오늘 네 종류를 각각 실측했다. **변경 종류마다 전이율이 다르다.**

| 변경 종류 | val Δ | Public Δ | 전이율 |
|---|---:|---:|---:|
| **CB 하이퍼** (`depth 5→6`) | +4.2 | +3.68 | **0.88** |
| 피처 추가 (`id_freq`) | +21.0 | +1.33 | **0.06** |
| 결합가중 — 합 이탈 (해로움) | −6.1 | −6.7 | 1.1 |
| 결합가중 — 합=1 안 미세조정 | +2.2 | −0.03 | **0.0** |
| **표현 교체 + 내부가중** (`sj_stdmlp`) | **+22.3** | **+1.68** | **0.075** |

> **08-28 정정 — 갈라지는 축은 「하이퍼 vs 피처」가 아니었다.**
> `sj_stdmlp` 을 `d5→d6` 의 환산율 0.834 로 예상해 **1091~1099** 라 적었는데
> 실측은 **1082.106 (+1.68)**, 전이율 **0.075** 다. 피처(0.06)와 같은 층이다.
> `d5→d6` 는 *모델 변경*이 아니라 **같은 피처·같은 구조에서 용량만 바꾸는 것**이고,
> 전이되는 것은 그것뿐이다. **학습 대상·표현·피처가 바뀌면 전부 0.06~0.08 로 떨어진다.**
> 0.834 를 모델 교체에 쓰지 않는다.

**따라서 순서는 하이퍼 → 멤버 구성 → 표현·피처 → 결합가중이다.**
결합가중 미세조정은 기대값이 0 으로 실측됐으므로 **더 하지 않는다.**

---

## 0.5 최종 상태 (08-28 완료)

**cw 3멤버 구성이 포화다.**

    cb   176열 원시값        트리 — 순서만 씀
    ft   168열 분위수 순위    토큰당 1열
    mlp  176열 z 점수 + 마스크  크기 보존

표현 축이 한 번씩 다 덮였고, 4번째를 무엇으로 넣어도(선형 / LGB / 다른 전처리 NN)
Δ24 ±0.8 · Δ22 전부 음수다.

**세 법칙**

1. **다양성은 피처가 아니라 표현에서 온다.** `id_freq` 를 `mlp` 에 넣는 것만으로는
   효과가 정확히 0 이었고(`q64:176`), z 점수로 표현을 바꾸니 +12.3 이었다.
2. **표현 레버는 표현당 한 번이다.** 한 멤버가 그 표현을 잡으면 소진된다.
   `ft` 를 z 점수로 바꾸면 단독이 두 폴드 최고인데(+22.0/+41.6) 결합 이득이 0 이다.
3. **다른 표현이면서 단독 신호도 있어야 한다.** 둘 중 하나만으로는 안 된다.

**3 의 근거 — 네 번째 표현 축 (항목 I).** 현행 3멤버는 전부 행 단위 **단조 변환**
(원시값/순위/z점수)이다. 단조가 아닌 축으로 **투수별 z 점수**(그 투수 자신의 분포
대비 특이도)를 시험했다. 트리도 전역 z 도 만들 수 없는 값이다.

    pz    rho 0.3960  <- 캠페인 최저. 관계적 표현이 구조적으로 다른 것은 맞다
          단독 -535.6 <- 그런데 신호가 없다

투수별 정규화가 **성공을 예측하는 정보 자체(투수의 수준)를 지우기** 때문이다.
수준을 되살린 `pzc`(전역z 352 + 투수별z 177 = 529 입력)는 두 폴드 다 양수지만
(+0.5/+0.4) 1 BSS 미만이다. 단조 대조군 `plog` 도 무효(가중 −0.069).

**즉 "가장 덜 상관된 표현"을 찾는 것 자체는 목표가 될 수 없다.**

## 1. 실행 순서

| # | 항목 | 시간 | 종류 | 상태 |
|---|---|---|---|---|
| **A** | CB 깊이 연장 `d7·d8` | 15분 | 하이퍼 | ✅ **완료 — `d6` 유지** |
| **B** | end-to-end 역할 조정 | 3시간 | 구조 | ✅ **채택 — `sj_e2var`, +3.1** |
| **C** | `mlp` → `cb_f_only` 판정 확정 | 1시간 | 멤버 구성 | ✅ **완료 — 기각** |
| **D** | [축2] prep 변환 | 1시간 | 구조(MLP) | ✅ **채택 — `sj_stdmlp`, +22.3 (캠페인 최대)** |
| **E** | F 부분군 라우팅 + 수축 | 1시간 | 결합(구조적) | ✅ **완료 — 생존하나 소이득, 미채택** |
| **F** | `ft` × id_freq × 표현 | 2.8시간 | 구조 | ✅ **기각 — 단독 +22 인데 결합 0** |
| **H** | [축7] 재검증 (신설) | 1시간 | 방법론 | ✅ **ρ 0.994 선은 오염의 산물이었다** |
| **I** | 네 번째 표현 축 (신설) | 1시간 | 구조 | ✅ **관계적 표현 — 실재하나 신호 없음** |
| **J** | [축1] 원자 조합 재판정 (신설) | 1시간 | 피처 | ✅ **`id_freq` 단독이 최선** |
| **K** | 마스킹 증강 (신설) | 1시간 | 학습데이터 | ✅ **기각 — 라벨과 어긋난다** |

### J·K — 지시받은 경로를 소진했다

**J.** `preprocess_lab/RESULTS.md` 가 "1위 조합 `id_frequency+temporal_cyclic+
trackman_quality` = +16.52, 단독 `id_frequency` +7.94 — 조합이 두 배를 넘겼다"
고 적었는데 배포본엔 `id_freq` 하나뿐이라 깨끗한 계기로 재판정했다.

| 조합 | cb단독24 | Δ24 | Δ22 |
|---|---:|---:|---:|
| **`id_freq`** | **856.4** | **기준** | **기준** |
| `+temporal` | 840.7 | −2.9 | **−54.2** |
| `+tm_quality` | 854.5 | −0.7 | −0.2 |
| `+temporal+tm_quality` (랩 1위) | 842.1 | −1.4 | **−44.5** |
| `+context` (랩 3위) | 847.9 | −0.7 | −36.8 |
| `+rate_geom+tm_quality` (랩 5위) | 851.1 | −2.6 | +0.8 |

**범인은 `temporal` 이다** — 들어간 arm 전부가 val2022 에서 −37~−54.
월·요일 sin/cos 와 "남은 연·월" 은 **절대 시간 정보**라 시즌을 건너뛰지 못한다
(학습 2019~2021 → 평가 2022, 그 사이 COVID 시즌이 낀다).
랩은 fold2024 만 봤고 **2022 비하락 관문이 그걸 잡았다** — `d6_l3k` 때와 같다.

**K.** `three_way/NEXT_PLAN.md` 의 "원본 + 마스킹 사본을 함께 학습하면 이득"
(reverse clip995, f23 853.0→868.7)을 성공 타깃에 이식했다. way 분해 자체는
S3 게이트에서 닫았지만 이 관찰은 way 와 무관하다.

`asof_pitcher_*` 16열을 지운 사본을 20/40% 덧붙였더니 **Δ24 −3.9~−8.5 ·
Δ22 −20~−22** 로 전부 기각. 비율이 클수록 더 나쁘다.

**기전**: 이력을 지운 사본은 **이력이 없는데 라벨은 그 투수의 진짜 실력을 반영**한다.
"실력 정보 없이 실력에 좌우되는 결과를 맞혀라" 를 배우니 잡음을 학습한다.
`id_freq` 가 성공한 것은 상황을 **표시만 하고 학습 신호를 건드리지 않기** 때문이다.
| **G** | [축6] 279 ∪ 168 | — | 피처 | ✅ **닫음 — 저장소 내 직접 증거** |

### 08-28 후반 결과

**D — 캠페인 최대 이득.** `mlp` 가 원시 계기에서 가중 0.00 인 이유를 밝혔다:
분위수-순위 변환은 **순서만 남기고 크기를 버리는데** 트리도 순서만 쓰므로
순위 MLP 는 cb 와 정보가 겹친다. 크기를 보존하는 z 점수로 바꾸니 가중 0.16 이 붙고
배포 순서 기준 val2024 **+22.3** · val2022 **+11.0**. `id_freq` 를 넣는 것만으로는
효과 0(`q64:176`) — **바뀌어야 하는 건 피처가 아니라 표현이었다.**

**E — hw 의 F 발견은 방향만 맞았다.** F 에서 hw 단독 614.8 vs cw 462.4 (+152) 이고
ρ(cw,sj3way) 0.6438 로 팀 최저인데도, F 에 hw 를 편입하면 정방향 +8.7 /
**역방향 −5.9** 다. 역방향 검정 유효성도 확인했다 — F행이 적합 17,256 / 평가 12,754 로
충분하고 F 성공률도 분할 간 +0.0014 로 안정적이다. R/F 라우팅 자체는 λ 0.3~0.7
수축으로 양방향 생존(+0.8/+2.3)하나 이득이 작아 미채택.

**F — 단독은 오르는데 결합이 0 이다.** `ft` 를 z 점수로 바꾸면 원시 단독이
val2024 755.6→**777.6**, val2022 2328.5→**2370.1** 로 두 폴드 다 최고인데
결합 Δ 는 −0.1 / +0.3 이다. **표현 레버는 한 멤버가 그 표현을 잡으면 소진된다** —
`std_mlp` 이 이미 z 점수를 담당하므로 ft 를 바꿔도 중복이다.
`id_freq` 만 넣는 것(`q64:176`)은 val2024 −21.8 로 오히려 해롭다 (mlp 때와 같다).

**H — [축7] 재검증. 캠페인의 판정 근거가 틀렸다.**
"단독 ~ ρ(cb) 상관 = +0.994 (21멤버), 정확하면서 탈상관은 선택지에 없다" 는
**오염의 산물**이었다. 원시로 재보면 ρ 가 0.75~1.00 으로 넓게 퍼진다.
그렇다고 **ρ 가 낮다고 좋은 것도 아니다** — 4번째 멤버 후보 넷 중 ρ 최저인
`lin_std176`(0.751)이 가중 −0.014 로 사실상 0 이고, 채택된 `std_mlp` 은
`q64_mlp` 보다 ρ 가 **더 높다**(0.8924 vs 0.8752).

**결합 유용성을 예측하는 것은 ρ 가 아니라 "다른 멤버가 구조적으로 접근할 수 없는
정보를 담은 표현"이다.**

**G — 실행하지 않고 닫았다.** 저장소 안에 직접 증거가 있었다:
`REVIEW_2026-08-24.md:312` **"피처를 186~279개에서 100개로 줄여도 성능이 같다
(861.5 vs 860.9). 정보가 이미 포화."** 여기에 §26 의 이식 5/5 기각, 실측 피처
전이율 0.06, 그리고 D 의 결론(다양성은 표현에서 온다)이 겹친다. 축6 의 전제는
**방향이 반대**다 — 두 트랙에 같은 피처를 먹이면 두 모델이 더 닮는다.

### 08-28 실행 결과 요약

**★ 계기 오염을 잡은 것이 가장 큰 소득이다.** `run_arm.py:307` · `run_family.py:343`
이 저장하는 예측은 `calib(p, yv)` 의 출력이고, `calib` 는 **평가 폴드 라벨로**
로짓을 재중심화하고 스케일을 고른다. 같은 `d6_l10k` 가 **저장 904.1 / 원시 851.3**
으로 갈렸다. 이후 모든 판정을 원시 예측으로 다시 했고, 그 결과 오늘 "채택"이라
불렀던 `cb_f_only` 가 뒤집혔다.

**환산자 확립** — 리더보드로 검증된 `d5→d6` 가 원시·고정가중 계기에서
val2024 **+4.0** → Public **+3.71** (**환산율 0.94**). 그 변경 자체가 이 계기의
val2022 에서 **−1.7** 이었으므로 **관문은 같은 계기 안에서 읽어야 한다.**

| 항목 | 결과 |
|---|---|
| A | `d7` 906.2 · `d8` 908.1 로 val2024 는 계속 오르지만 **val2022 가 −1.6 / −10.7**. 깊이와 두 폴드가 완전 역상관이고 `d6` 만 동시 만족. **현 제출본이 이미 최적** |
| B | **오차가중은 무너지고(−214~−14238) 시드분산가중은 된다(+3.1).** ρ 선은 오차가중이 뚫었지만(0.40→−0.33) 신호가 남지 않았다 — D4 에 이은 두 번째 확인. 채택안 `sj_e2var` |
| C | **`cb_f_only` 기각** — 원시 계기에서 val2022 −64.6. 오늘 "3.42σ 채택안"이라 부른 것은 오염된 계기의 산물이었다 |
| 팀 멤버 재검증 | 오염이 "hw·yn 가중 0" 을 만든 것인지 확인 — 전원 공평 보정 후에도 **결론 유지**. 부수 발견: yn·hw 는 스케일이 잘못돼 있다 (보정만으로 +30.8 / +26.0) |

---

## A. CB 깊이 연장 — 진행 중

1차 격자에서 **val2024 최적이 `d6` 모서리**에 붙었다. `l2 3000` 계열은 val2022
관문에서 전부 탈락했으므로 **`l2 10000` 이상에서만** 연장한다.

    arm   g_d7_l10k · g_d8_l10k · g_d7_l30k · g_d8_l30k     3짝시드 × 2폴드

중간 결과 `d7_l10k` 단독 **906.2** (d6_l10k 904.1, d5_l10k 900.3) — 계속 오른다.

**멈춤 조건**: 추론 시간. 오블리비어스 트리는 깊이만큼 경계 비교를 하므로
비용이 깊이에 선형이다 (`d5` 88.7초 → `d6` 105.2초, 253K행). 서버 388/600초에서
`d8` 은 약 140초로 추정되고 최악 가정에서도 예산 안이지만, 이득이 멎으면 멈춘다.

**판정**: val2024 주판정 + val2022 비하락. `d6_l3k` 를 이 관문이 걸렀다.

---

## B. end-to-end 역할 조정 — ✅ 채택 (`sj_e2var`)

**결과는 `models/sj_e2var.md` 에 있다.** 요지만 적는다.

- **오차가중(E1/err) 기각** — `(y-p)²` 가 큰 행은 줄일 수 있는 오차가 큰 곳이 아니라
  **라벨이 동전던지기인 곳**이다. 예측 분산이 라벨 분산의 0.86% 뿐이라
  오차가중 = 잡음 추종. cb2 단독이 −214 ~ −14,238 로 무너졌다.
- **시드분산가중(E2/var) 채택** — 라벨을 보지 않고 **모델이 흔들리는 곳**만 겨냥한다.
  `alpha 0.15` 에서 val2024 **+3.1** · val2022 **+0.3**, 추론 비용 **0**.
- ρ 선은 오차가중만 뚫었고(0.40 → −0.33) 신호가 남지 않았다.
  **D4 에 이은 두 번째 독립 확인** — 그 선을 벗어나는 것 자체가 목표일 수 없다.

아래는 착수 당시의 설계다 (기록용).

## B(원안). end-to-end 역할 조정

**문제 의식**: 지금은 멤버를 **따로 학습해 놓고 나중에 섞는다.** 각 멤버는 자기
혼자 잘하려고 학습됐지 **앙상블 안에서 무슨 역할을 맡을지 모른 채** 학습된다.
그래서 `단독 성능 ~ ρ(cb)` 선(r=+0.994, 21멤버)에 전부 붙어버렸고, 그 선을 벗어난
유일한 방법이 "다른 학습 데이터"(F행만/최근시즌만)였다.

**앙상블 결과를 학습 신호로 되먹이면** 멤버가 스스로 역할을 나눌 수 있다.
이건 피처가 아니라 **구조** 변경이므로 전이율이 높은 쪽이다.

### E1 — 앙상블 잔차 부스팅

```text
1. p_ens = r + Σ w_i (p_i − r)          w 는 동결 (합=1)
2. 새 CatBoost 를 타깃 (y − p_ens) 로 학습        <- 앙상블이 못 맞힌 부분만
3. p_new = p_ens + η · correction                η ∈ {0.1, 0.3, 0.5}
4. 정직 판정: 월전방분할 양방향
```

**위험**: 스태킹이라 과적합하기 쉽다. `w` 를 적합한 fold 에서 잔차를 만들면
그 fold 의 우연을 학습한다. **반드시 `w` 적합 fold 와 잔차 학습 fold 를 분리**한다.

### E2 — 상보 표본가중 재학습

```text
1. 각 멤버 i 에 대해 **자기를 뺀** 앙상블 p_{-i} 를 만든다
2. 그 앙상블의 행별 오차 e_i = (y − p_{-i})^2 로 표본가중을 준다
3. 멤버 i 를 그 가중으로 재학습          <- "남들이 못하는 곳을 맡아라"
4. 새 멤버들로 w 를 다시 적합하고 정직 평가. 2~3회 반복
```

E2 가 **"각 모델이 역할을 조정한다"** 는 요구에 더 가깝다. 오늘 확인된
"다양성을 만드는 유일한 레버는 다른 학습 데이터"와도 맞는다 — 표본가중은
학습 데이터를 바꾸는 가장 부드러운 방법이다.

**멈춤 조건**: E2 는 반복마다 전 멤버를 재학습하므로 비싸다. 1회 반복 후
정직 Δ 가 음수면 멈춘다.

---

## C. `mlp` → `cb_f_only` — 계기 충돌 해소

**두 측정이 어긋난다. 원인 규명이 먼저다.**

| 계기 | val2024 | val2022 |
|---|---:|---:|
| 블렌드 재적합 (`refit_cw_weights` 계열) | 911.5 vs 906.9 (+4.6) | 2492.1 vs 2488.3 (+3.8) |
| 시드예산 재측정 (`seed_budget.py`) | +6.8 | **−2.9 (관문 탈락)** |

**cb 절대값 자체가 어긋난다** — 같은 `d6_l10k` 구성인데 906.9 vs 877.0.
`seed_budget.py` 는 cb 를 새로 학습했고 앞쪽은 `GRID_idfreq` 의 저장된 예측을 썼다.
차이 후보: (a) `atoms.build` 의 `train_mask` 인자, (b) EPS 클리핑, (c) 시드 정렬.

**먼저 두 경로가 같은 입력에서 같은 예측을 내는지 확인**하고, 그 다음 판정한다.
이 불일치는 다른 결론들도 오염시킬 수 있으므로 우선순위가 높다.

추론 비용도 같이 본다 — CatBoost 한 벌이 더 붙어 cb 구간이 2.19배가 된다.
넣으려면 **cw 모듈 자체의 시간 분해**(cb/ft/mlp 각 몇 초)가 먼저다.

---

## D. [축2] prep 변환

MLP 가 168 을 **어떻게 보느냐**가 통째로 미탐색이다 (§30.2). 원래 이 축을 쳐낸
이유는 "`mlp` 를 블렌드에서 빼는 게 이득(−7.2)이라 대상 소멸"이었는데,
`id_freq` 와 `depth 6` 으로 cb 가 강해지면서 **블렌드 구성이 달라졌다**.

새 cb 위에서 `mlp` 가중은 val2024 −0.02 / val2022 0.18 이다. **2024 에서 죽어 있다.**
살릴 수 있는지, 아니면 C 로 대체할지를 이 축이 정한다.

    MLP 8 arm × 8시드 (시드당 44초) ≈ 1시간

---

## E. F 부분군 라우팅 — hw 의 발견을 받아서

hw 가 `cowork/hw/hw_sj_segmented_blend_analysis.md` 에서 보고한 것:

> **★ game_type=F 에서 상관이 0.748 로 뚝 떨어지고, 이득이 +56.1 로 전체 중 최대**

**재현 결과 — 부분군 상관 (val2024, 현행 멤버)**

| 부분군 | n | ρ(cw,sj3way) | ρ(hw,sj3way) | ρ(hw,cw) |
|---|---:|---:|---:|---:|
| R | 223,497 | 0.9069 | 0.9555 | 0.9217 |
| **F** | 30,010 | **0.6430** | 0.8879 | 0.7520 |

**hw 의 방향은 맞았지만 주인공이 다르다.** F 에서 가장 탈상관인 쌍은 hw 가 아니라
**이미 결합에 들어가 있는 `cw × sj3way`(0.643)** 다. 팀 전체 최저값이다.

**라우팅 실측 (월전방분할 양방향)**

| 구성 | 정방향 | 역방향 |
|---|---:|---:|
| 라우팅 없음 | 727.2 | 1059.6 |
| `F:[cw,sj,hw]` | 735.7 (**+8.5**) | 1053.4 (**−6.2**) |
| `F:[hw,sj]` | 738.6 (**+11.3**) | 1053.4 (**−6.2**) |

**볼카운트 때와 같은 패턴이다** — 정방향만 크고 역방향이 음수. 그때 λ=0.3 수축으로
양방향 생존(+1.5/+1.2)했으므로 **같은 처방을 적용해 판정**한다.

F 는 행의 11.8% 지만 멤버 간 BSS 폭이 68 로 R(1.9)의 36배인 **전장**이다.

---

## F. `id_freq` 완전 스택 2폴드

`S1_idfreq` 를 FT 단계에서 중단시켰다. 지금 있는 건 **cb 단독**이다.
제출본은 cb 만 바꿨으니 실무상 문제는 없으나 **채택 기록이 미완**이다.

    FT 시드당 600~700초 × 8시드 × 2폴드 ≈ 3시간

**이득이 아니라 기록 완결이므로 우선순위가 낮다.** GPU 유휴 시간에 돌린다.

---

## G. [축6] 279 ∪ 168 — 기대값이 가장 낮다

계획서(§29.2)가 이미 **"기대부호는 마이너스"** 라 적었고, 부분적으로 이미
다섯 번 실패했다 (§26: cw 원자 5종을 sj 279 로 이식 → 전부 기각).

여기에 **오늘의 전이율 실측이 더해진다 — 피처 전이율 0.06.** val 에서 +21 을
얻어도 Public 에서 +1.3 이다. sj 279 행렬 재생성(3시간+)의 값어치가 없다.

**판정: 다른 항목이 전부 끝나고 GPU 가 남을 때만 한다.**

---

## 2. 팀에 요청할 것

| 대상 | 요청 | 왜 |
|---|---|---|
| **ye** | `oof_predictions.csv` (walk-forward OOF) + 모델 파일 | `cowork/ye/champion_structural_improvement.ipynb` 는 **완전히 독립된 파이프라인**(platoon/hand 중심 CatBoost + shallow LGB)인데 **산출물이 저장소에 없다** (출력 삭제됨, 모델은 맥 로컬 경로). 피처셋이 우리와 달라 **팀에서 가장 탈상관일 가능성이 있는 미확인 자산**이다 |
| **hw** | `<=2021` 학습 val2022 예측 | 규칙 1 의 비하락 관문을 hw 에 적용할 수 없다 |
| **yn** | 같은 것 + **학습 코드 전체** | 관문 + RULES §7 의 09.11 재현 검증. `models/yn_fa10c/MISSING.md` |

**ye 가 가장 값나갈 수 있다.** 우리 세 모델(cw·sj·hw)은 전부 ρ 0.89~0.96 으로
묶여 있는데, ye 는 피처 생성 자체가 다르다.

---

## 3. 하지 않을 것 (실측 근거)

| 항목 | 근거 |
|---|---|
| 결합가중 미세조정 | 합=1 안에서 `w_cw` 0.41~0.64 무차별. Public 실측 0.026 |
| `yn`·`hw` 전역 편입 | 비음수·합=1 적합에서 **가중 정확히 0** |
| 볼카운트 구간 가중 | 역방향 −2.0. 수축해도 +1.2 로 작다 |
| 투구표본수 구간 가중 | ~0. 챔피언 작성자 보고와 독립 재현 일치 |
| 점수차 구간 가중 | −8.8 |
| 시드 8개로 증량 | Δ(8−3) 이 두 폴드 모두 음수 |
| 오차 상관 기반 멤버 선택 | 예측 분산이 라벨 분산의 0.86% 라 모든 모델 오차 ρ ≈ 1 |

---

## 4. 앙상블 자산 관리

**결합을 다시 하려면 모든 멤버의 정직한 val 예측이 필요하다.** 이게 없어서
오랫동안 팀 결합을 재적합하지 못했다. 규약은 `README.md` 규칙 3, 협업 방법은
`COLLAB_final_submissions.md`.

현재 확보 상태:

| 멤버 | val2024 | val2022 | 비고 |
|---|---|---|---|
| `sj_grid_w060` (현행 cw 모듈) | ✅ | ✅ | 903.0 / 2490.2 |
| `sj3way` | ✅ | ❌ | 850.5 |
| `cw_v17_base` | ✅ | ✅ | 821.0 / 2498.5 |
| `yn_fa10c` | ✅ | ❌ | 815.6 |
| `hw_v12` | ✅ | ❌ | 808.9 |
| **`ye`** | ❌ | ❌ | **아예 없음** |

**새 멤버를 만들 때마다 `val/<name>_{2024,2022}.csv` 를 같이 낸다.**
도구는 `models/sj_grid_w060/` 에 전부 있다 (`team_blend.py` · `final_team_w.py` ·
`bucket_blend.py` · `set_blend.py` · `check_submit.py`).

---

## 5. [신설 2026-08-28] 부분군 진단 — `group_by_perform/`

**전역 피처의 전이율이 0.06 (§0) 인 이상, 다음 레버는 "어디서 잃는지"를 먼저
특정하는 것이다.** 시즌 전체 BSS 하나와 `R/F`·월블록만으로는 좌표가 안 나온다.

계획 전문: [`group_by_perform/PLAN.md`](group_by_perform/PLAN.md)

### 무엇을 하는가

등록된 val 예측 전부(8모델 + 팀결합)를 **13개 축으로 쪼개** `AUC` 와 `BSS` 를 같이 낸다.
연속형 축은 2022+2024 실분포(500,979행)를 재서 구간마다 `n ≥ 5,000` 이 되게 잘랐다.

| 지시받은 4축 | 원천 |
|---|---|
| A1 볼카운트 | `balls_before` × `strikes_before` 12칸 |
| A2 경기 중요도 | `li` 6구간 (마지막 절단을 통용 기준 LI 2.0 에 맞춤) |
| A3 전시즌 경기당 평균 대비 현재 등판 투구수 | 파생 — `game_uid` 복원 필요 |
| A4 전시즌 투구수 | 파생 — `없음` 구간이 **전체 행의 24.4%** |

확장 9축(cold-start · 투수 수준 · 이닝 · 손매치업 · 역할 등)은 계획서 §2.

### 이 진단이 새로 만드는 것

1. **`game_uid` 복원.** `train.csv` 에 경기 ID 가 없다. 파일 시간순 + 팀쌍 전환 +
   이닝 감소로 끊어 복원했고 검증했다 — 시즌당 796~824경기, 경기당 중앙 300구,
   등판당 중앙 19구 · p90 92구. 이걸로 A3·A12(역할)이 가능해진다.
2. **`deficit` 컬럼.** `Σ deficit = 100000 − BSS_all` 이 정확히 성립하게 정의했다.
   구간별 점수 결손의 몫이라 **구간끼리 직접 비교된다.** `bss_local` 만 보면
   작고 나쁜 구간에 속는다.
3. **`shift_gain_xfer`.** 구간 상수 보정으로 회수되는 점수를 §0 의 규약대로
   (2022 적합 → 동결 → 2024 평가) 낸다. `self` 이득은 채택 근거로 쓰지 않는다.

### 관문 (사전 고정)

| 판정 | 조건 |
|---|---|
| 처방 가능(보정) | `shift_gain_xfer ≥ 3.0` 이고 두 시즌 `bias` 부호 동일 |
| 처방 필요(모델) | `deficit` 상위 + `auc` 가 전체 대비 −0.010 이상 낮음 |
| 착시 | `deficit` 가 전체의 2% 미만 — 건드리지 않는다 |
| **멤버 분담** | 같은 구간에서 모델 간 `bss_local` 차 ≥ 30 — `bucket_blend.py` 축 후보 |

**실행으로 이어질 가능성이 가장 높은 것은 마지막 항목이다.** 나머지는 §3 "하지 않을 것"
에서 이미 기각된 종류(부분군 전용 피처)와 붙어 있다.

### A3 의 한계 — 미리 적는다

`game_pitch_no` 는 `test.csv` 의 다른 행을 봐야 만들어져 행 독립성 규칙에 걸린다.
**A3 에서 약점이 나와도 그대로 피처가 되지 않는다.** A4·A5 는 각각 전시즌
`train.csv` 집계와 공식 `asof_pitcher_n` 이라 피처로 써도 된다.

### 실행 결과 (같은 날 완료) — [`group_by_perform/RESULTS.md`](group_by_perform/RESULTS.md)

**넷 중 셋이 닫혔고, 하나가 열렸고, 계기 문제가 하나 나왔다.**

| 관문 | 결과 |
|---|---|
| 처방 가능(보정) | **미달.** 두 방향 다 양수인 축이 주요 모델엔 없다 |
| 멤버 분담 | **미달.** `cw_v17_base` 가 `sj_grid_w060` 을 30점 이상 이기는 구간 **0개** |
| 처방 필요(모델) | **통과 — 볼카운트 2스트라이크 / `0-2`** |
| 착시 | A6 `이력없음`(n=81) 등 제외 |

1. **★ 계기 — `2022 all` 관문의 70%가 `game_type` 이다.** 2022 F 기저율 **0.7087** ·
   R 0.5037 (2024 는 F 0.4593 · R 0.4897 로 격차 없음). `sj_grid_w060` 이
   **all 2,490.2 → R만 740.7**. 2024 는 903.0 → **921.4** 로 오히려 오른다.
   → **관문을 `2022 R` 로 바꿀 것을 제안한다.** 비용 0, 효과는 앞으로의 모든 채택 판정.
2. **볼카운트가 15축 중 1위**(격차 크기 151.9)이고 **유일하게 두 시즌 안정**이다.
   `0-2` 는 AUC 0.5323 / 0.5316 (전체 0.5488 / 0.5541), BSS 269 / 267.
   두 시즌 값이 거의 같다. 예측 표준편차도 그 구간에서만 15~22% 좁아진다 —
   **모델이 신호 없음을 알고 수축하고 있다. 보정이 아니라 새 정보가 필요하다.**
3. **`li`(경기 중요도)는 축이 아니다.** 15축 중 9위, 6구간 중 4구간 부호 반전,
   2024 AUC 폭 0.008 로 최소. **`li` 파생 피처는 만들지 않는다.**
4. **구간별 결합가중(`bucket_blend.py`)은 붙일 자리가 없다.** 전역으로 좋은 모델이
   거의 모든 구간에서 좋다. §0.5 의 "3멤버 포화" 를 구간 단위로 재확인한 것이다.

**다음 항목 (우선순위)**

| # | 항목 | 종류 | 근거 |
|---|---|---|---|
| ~~**L**~~ | ~~관문을 `2022 R` 로 교체~~ | 계기 | ✅ **채택·구현 완료 (08-28)** — 규칙 1 개정. 관문은 `2022 R` **와 `2023 R`**, 주 판정은 `2024 all`. `tools/common.py`·`score_val.py` 반영 |
| **M** | **카운트 조건부 `asof_*` 피처** (투수 × 카운트국면 7) | 피처 | RESULTS §3.1 |
| N | 8~10월 블록 진단 (가장 큰 안정 열세 −56/−88) | 진단 | RESULTS §4 |
| O | 스윙맨(전시즌 30~70구/경기) 역할 피처 | 피처 | RESULTS §4 |

M 은 피처라 전이율 0.06 이 걸린다. 그래서 **1차 관문을 전체 BSS 가 아니라
"2스트라이크 구간 AUC 가 오르는가" 로 잡는다** — 구간 AUC 가 안 오르면 전체가
올라도 다른 게 움직인 것이므로 채택하지 않는다.

### L 채택 직후 나온 것 — 재채점이 등록 판정 두 개를 뒤집는다

새 관문으로 등록 자산을 다시 돌리면 `results.csv` 의 `채택` 이 재현되지 않는다.

| 모델 | 2024 all | 2022 all (구) | **2022 R (신)** | 등록 | 재계산 |
|---|---:|---:|---:|---|---|
| `sj_e2var` | **−25.6** | −5.7 | −2.3 | 채택 | **기각** (2024 미상승) |
| `sj_stdmlp` | +13.0 | **−137.1** | **−72.6** | 채택 | **기각** (관문 하락) |

원인은 **기준 모델의 계기 차이**다. 두 `.md` 는 "배포 순서 재현" 기준을
`2024 893.7 / 2022 2,342.1` 로 적었는데 등록된 `val/sj_grid_w060_*.csv` 는
`903.0 / 2,490.2` 다 — 2022 에서 **148** 벌어진다.

**L 이 만든 문제가 아니다.** 구 관문(`2022 all`)으로 재도 `sj_stdmlp` 은 −137.1 로
기각이고, 이건 그 제출본의 Public 이 예상 1091~1099 에 못 미치고 **+1.68** 에 그친 것과
방향이 같다. **어느 계기가 맞는지는 미확인.** 규칙대로 기존 행은 손대지 않았다 —
`val/sj_grid_w060_2022.csv` 의 출처 확인이 다음 작업이다.

### 2차 실행 (같은 날) — 55모델 전반 + 구간별 결합가중 실측

**두 가지가 새로 확정됐고, 앞의 결론 하나가 뒤집혔다.**

#### 1. 약점은 모델이 아니라 자료다 — 55모델 예외 0

`val/` 의 두 시즌 예측이 다 있는 **모델 55개 전부**로 다시 돌렸다 (CatBoost 여러 깊이 ·
LGB 31/63/127잎 · dart · extra · huber · linear · XGB · FT · MLP · 증류 · way 분해 · 가중평균).

| 구간 | 두 시즌 모두 축 평균 미달 | gap_w 중앙값 22 / 24 |
|---|---:|---:|
| `0-2` 카운트 | **55 / 55** | −431 / −600 |
| 2스트라이크 | **55 / 55** | −222 / −241 |
| 전시즌 `1-200` | **55 / 55** | −450 / −377 |
| 스윙맨(30~70구) | **55 / 55** | −95 / −366 |
| 타자우위 카운트 | 0 / 55 (전부 **우세**) | +257 / +144 |

**예외 0개다.** 축 순위도 55모델 중앙값으로 그대로다 (볼카운트 149.7 > 카운트국면
145.9 > 전시즌투구수 143.2 > … > **li 81.8**). 모델을 바꿔서 될 일이면 55개 중 하나는
덜 졌을 텐데 없다 → **항목 M(카운트 조건부 피처)의 근거가 강해졌다.**

#### 2. ★ 구간별 결합가중은 **오른다** — 앞 결론 정정

앞에서 "`cw` 가 `sj_grid` 를 이기는 구간 0개 → 구간별 가중은 자리가 없다" 고 썼는데
**그건 틀린 추론이다.** 가중은 순위가 아니라 오차 상보성으로 정해진다. 직접 쟀다
(`group_by_perform/bucket_blend.py` · `bucket_ci.py`, 한 시즌 적합 → 동결 → 다른 시즌 평가,
행 부트스트랩 400회).

| 축 | strong 2024 all | core 2024 all | strong 2022 R |
|---|---|---|---|
| **A6 투수 수준 5분위** | **+2.86** `[+0.8, +5.2]` ★ | **+5.32** `[+2.7, +7.9]` ★ | **+10.19** `[+7.6, +12.9]` ★ |
| A8 손 매치업 | +4.50 ★ | +3.06 ★ | +1.87 |
| A1 볼카운트 | −1.91 | +3.49 ★ | +7.08 ★ |
| A5 투수 누적 이력 | **−4.26** ✗ | — | — |

**`A6` 만 네 칸 × 두 멤버집합 × 세 신뢰구간 전부 양수다.** 나머지는 멤버를 바꾸면
뒤집힌다 — 축의 성질이 아니라 그 조합의 구간별 치우침을 메우는 것이다.
`A6` 가 되는 이유는 §0.5 의 세 표현축(원시값 / 순위 / z점수)이 **투수 수준을 서로
다르게 표현**하기 때문으로 본다. 상관 0.9892 는 평균의 이야기지 구간별 이야기가 아니었다.

**항목 P (신설) — `A6` 구간가중 제출 1회.** 재학습이 필요 없다
(`models/sj_grid_w060/set_blend.py` 가 `blend_weights.json` 만 교체한다).
결합층 변경의 실측 전이율이 0.0 이라 val 로 더 다투는 것보다 **Public 한 번이 싸다.**

    축      A6 투수 수준 5분위 (asof_pitcher_success_rate 20/40/60/80 분위)
    수축    lambda = 0.5
    되돌림  구간 행수 2,000 미만이면 전역 가중
    멤버    sj_stdmlp · sj_grid_w060 · cw_v17_base · sj_cb_ft_fonly

**하지 않는다**: `A11` 구간가중(core 에서 +13.4 지만 2022 F 기저율을 메우는 것이고
strong 에서 +0.02) · `lambda=1.0`(관문 칸 부호가 흔들린다) · 여러 축 동시 적용.

#### 갱신된 순서

| # | 항목 | 종류 | 상태 |
|---|---|---|---|
| **L** | 관문을 `2022 R` 로 교체 | 계기 | ✅ **완료** — `tools/common.py` 개정 반영됨 |
| **P** | **`A6` 구간별 결합가중 제출 1회** | 결합(조건축) | **다음** |
| **M** | 카운트 조건부 `asof_*` 피처 | 피처 | 대기 (55모델 근거 확보) |
| N | 8~10월 블록 진단 | 진단 | 대기 |
| O | 스윙맨 역할 피처 | 피처 | 대기 |

### 항목 P 판정 — **기각.** 정정한다

배포로 옮기려다 죽었다. `group_by_perform/RESULTS.md` §9 전문.

**배포 계기는 이미 구간별 결합을 지원한다.** `blend_weights.json` 에 `bucket_edges` /
`buckets.<label>.w` 가 있고 `script.py` 168행이 `np.digitize(asof_pitcher_n, edges)` 로
가른다. 축이 **A5(투수 누적 이력)로 하드코딩**돼 있고, 그 축은 배포 쌍에서
**유의하게 해롭다** (2024 all −2.33 `[−4.49, −0.16]`). 챔피언 작성자가 접은 판단이 옳았다.

**A6 로 축을 바꿔도 안 된다.** 기준선을 리더보드가 검증한 `w={cw 0.6, sj 0.4}` 로
고정하고 버킷 구조만 바꾸면 **네 칸 전부 · 모든 λ 에서 진다** (λ=0.25 −0.8~−1.5,
λ=1.0 −5.7~−13.4, λ 단조).

**왜 §5 에서는 올랐나 — 기준선이 나빴다.** A6 구간별 `cw` 가중:

    fit2022   Q1 0.673  Q2 1.000  Q3 0.436  Q4 0.591  Q5 0.892
    fit2024   Q1 0.222  Q2 0.070  Q3 0.000  Q4 0.000  Q5 0.237

구간 간 변동(σ 0.10~0.24)보다 **시즌 간 변동(0.6+)이 3~6배 크다.** 기준선을 같은
방식으로 재적합하면 그 틀린 수준이 양쪽에서 상쇄돼 작은 모양만 이득으로 남는다.
고정 가중 대비로 재면 틀린 수준이 그대로 들어간다.

> **법칙**: 구간별 결합가중의 이득은 **그 조합에서 실제로 쓸 기준선** 대비로만 잰다.
> 재적합 기준선 대비로 재면 기준선의 전이 오차가 이득으로 둔갑한다.
> 이번에 두 층에서 나왔다 — 약한 멤버(§5 `core`) · 재적합 전역가중.

`blend_weights.json` 은 `{cw 0.6, sj 0.4}` 단일 구간 그대로 둔다.
**남은 레버는 항목 M 하나다.**

| # | 항목 | 상태 |
|---|---|---|
| L | 관문 `2022 R` 교체 | ✅ 완료 |
| ~~P~~ | ~~A6 구간별 결합가중~~ | ❌ **기각** (RESULTS §9) |
| **M** | **카운트 조건부 `asof_*` 피처** | **진행 중** — 55모델 근거(§7)가 여기 있다 |
| N | 8~10월 블록 진단 | 대기 — §6.6 참조 |
| O | 스윙맨 역할 피처 | 대기 — §6.6 참조 |

---

## 6. 남은 4일 실행 계획 — 2026-08-28 저녁 확정

### 6.0 우선순위를 정하는 논리

**리더보드에서 살 수 있는 것이 +0.5 인데, 09.11 검증 탈락은 Phase 3 전체다.**
저울에 올리면 비교가 안 된다. 그래서 순서를 이렇게 잡는다.

    ① 계기 규명   판정의 신뢰성       ← 이게 없으면 ②의 판정이 무의미하다
    ② 항목 M 제출  기대 +0.5, 슬롯 무료
    ③ 09.11 blocker  Phase 3 진출 여부   ← 실질 최우선
    ④ val2023 4개  새 관문을 작동시킨다
    ⑤ 동결·PPT

### 6.1 ① 계기 불일치 규명 — 최우선

`val/sj_grid_w060_2022.csv` 는 **2,490.2**, 같은 모델의 배포순서 재현은 **2,342.1**.
**148 이 벌어진다.** 어느 쪽이 맞는지 아직 모른다.

**이게 M 의 판정을 직접 흔든다.** M 의 `2022 R` 이 base 에 따라 `+2.58 / −1.55` 로
부호가 갈리는데, 그 기준선이 어느 계기인지에 따라 관문 통과 여부가 달라진다.
**지금 상태로는 M 을 "채택" 이라 부를 근거가 없다.**

> **항목 P 가 오늘 정확히 이 함정에서 두 번 뒤집혔다.** `+2.9~+5.3` 으로 채택했다가
> 그 이득이 **나쁜 기준선의 전이오차**였음이 드러나 기각됐다 (§9).
> **기준선이 흔들리면 이득이 아니라 기준선의 오차를 재게 된다.**

할 일: `val/sj_grid_w060_2022.csv` 가 어떤 구성·어떤 계기로 만들어졌는지 확인하고,
배포순서 재현과 어긋나는 지점을 특정한다. 규칙대로 `results.csv` 기존 행은 손대지 않는다.

### 6.2 ② 항목 M — 배포 재굽기 후 Public 1회

**관문 판정을 정직하게 적는다.**

| 관문 | 결과 |
|---|---|
| 1차 (사전 고정) 2스트라이크 AUC 두 시즌 상승 | ❌ **실패** (2022 −0.0001) |
| 2차 2024 all 상승 | ✅ +7.6, CI 가 0 을 넘지 않음 |
| 2차 2022 R 비하락 | ⚠ 구성에 따라 갈림 (+2.58 / −1.55) |

**사전에 고정한 1차 관문은 통과하지 못했다.** 다만 2022 의 −0.0001 은
"나빠졌다" 가 아니라 **"아무 일도 없었다"** 로 읽는 것이 자료에 맞다 —
관문 문구가 *없음* 과 *실패* 를 구분하지 못했다. **다음 관문을 쓸 때 고칠 점이다.**

**기대값을 부풀리지 않는다.**

    val2024 +7.6  x  피처 전이율 0.06  =  Public +0.5

**올리는 이유는 슬롯이 무료이고 이것이 마지막 레버라서지 점수를 기대해서가 아니다.**
1082 가 1083 이 되면 성공이다. 절차:
`build_final_cb.py` 재굽기(depth 6 + 상보가중) → zip 재조립 → `check_submit.py` → 제출 1회.

### 6.3 ③ 09.11 검증 blocker 둘 — 실질 최우선

| 대상 | 없는 것 | 없으면 |
|---|---|---|
| **yn** | `val2022`·`val2023` + **학습 코드 전체** | 최종 패키지에서 제외 |
| **ye** | `models/ye_hand/` **학습 코드** (val 3시즌은 sj 재현본으로 보유) | 동일 |

**hw 는 완료했다** — `val2022`·`val2023` 둘 다 도착했고 관문 적용 결과 기각으로 판정났다.

### 6.4 ④ `val/*_2023.csv` 네 개

`sj_grid_w060` · `sj_stdmlp` · `sj_e2var` · `cw_v17_base` **전부 2023 이 없다.**
`hw_v12` · `hw_v12_honest` · `ye_hand` · `sj3way_nv` 는 있다.
**정작 판정 기준이 되는 sj 모델들이 비어 있어 새 관문의 절반이 작동하지 않는다.**
`<=2022` 학습으로 한 번씩 돌린다.

### 6.5 일정

| 날짜 | 할 일 |
|---|---|
| **08.28 금(밤)** | ① 계기 규명 · ② M 재굽기 착수 · ③ yn·ye 에 못 박기 |
| **08.29 토** | ② M 제출 · ④ val2023 4개 · ③ 코드 수령 |
| **08.30 일** | 최종 후보 확정 · `check_submit.py` · 서버 시간 측정 |
| **08.31 월** | ★ **동결.** PPT 착수 (`DECK.md` 를 뼈대로) |
| **09.01 화** | 최종 제출 |
| 09.02~06 | 코드 패키지 + PPT |
| **09.07** | 코드·PPT 제출 |

**월요일의 목표는 점수가 아니라 동결이다. 마감 당일에 조립하지 않는다.**

### 6.6 하지 않을 것

- **구간별 결합가중** — 항목 P 기각 (§9). 시즌 간 변동이 구간 간 변동의 3~6배다.
- **N(8~10월) · O(스윙맨) 피처화** — 채울 빈칸이 특정되지 않는다. 남은 시간에 맞지 않는다.
- **팀 결합가중 재조정** — 합=1 안에서 무차별, Public 실측 0.026.


### 항목 M 실행 결과 — 2024 에서 겨냥한 자리가 올랐다. 2022 는 무변화

계기 `cowork/sj/sj_final/src/build_countphase.py` 신설 · `run_arm.py --extra cntdev`.
전문은 `group_by_perform/RESULTS.md` §10.

**설계**: 시즌 S 의 행은 시즌 < S 만으로 만든 LUT 를 본다(**시즌 동결**). 2025 에서
하게 될 일과 정확히 같아 학습·추론 분포가 어긋나지 않는다.

**학습 전 부분선별**: 기존 투수 성공률 4열 통제 후 남는 부분상관이 `0-2` 에서
`cph_dev_self` +0.021 · `cph_2s_dev` +0.025 인데 그 외 구간은 1/3 수준이다.
수준형 열은 0 근처 → 버렸다. 편차 3열만 썼고, **이 선별이 실측으로 확인됐다**
(전체 6열은 2024 −0.9 · 2022 R −2.7).

**잡음 바닥**: 이 arm 은 재실행이 결정론적이다 (ΔBSS +0.0000). §3.5 의 ±2 는
depth 6 + 시드분산 상보가중 구성 얘기고 여기 적용되지 않는다.

| base | 2024 all | 2024 R | 2022 R |
|---|---:|---:|---:|
| 168피처 | **+7.16** `[+2.9, +11.8]` ★ | +7.68 ★ | +2.58 (0 포함) |
| 176피처 `id_freq` | **+7.64** `[+3.3, +12.1]` ★ | +7.46 ★ | −1.55 (0 포함) |

**`id_freq` 가 흡수하지 않는다** — 배포 피처 집합에서도 +7.6 이 남는다.
구간별로는 **`0-2` 가 +41.3 BSS · AUC +0.0016 으로 전 구간 최대**다. §7 에서
55모델이 전부 지던 칸이고, 부분상관이 가장 컸던 칸이다.

**관문 판정**

| 관문 | 결과 |
|---|---|
| 1차(사전 고정) 2스트라이크 AUC 두 시즌 상승 | ❌ **실패** (2022 −0.0001) |
| 2차 2024 all 상승 | ✅ +7.6, 신뢰구간이 0 을 넘지 않음 |
| 2차 2022 R 비하락 (규칙 1) | ⚠ 구성에 따라 갈림 (+2.58 / −1.55, 둘 다 0 포함) |

데이터가 말하는 것은 "2022 에서 나빠졌다" 가 아니라 **"2022 에서는 아무 일도 없었다"**
다. 관문 문구가 "없음" 과 "실패" 를 구분하지 못했다 — 다음 관문을 쓸 때 고칠 점이다.

**val 로는 여기가 끝이다.** 피처 전이율 0.06 이고 2022 관문이 구성에 따라 갈리는
이상 val 을 더 파도 갈리지 않는다. 다음은 배포 구성(depth 6 + 상보가중)으로
`build_final_cb.py` 재굽기 → zip 재조립 → **Public 1회**.

| # | 항목 | 상태 |
|---|---|---|
| L | 관문 `2022 R` 교체 | ✅ 완료 |
| ~~P~~ | ~~A6 구간별 결합가중~~ | ❌ 기각 (RESULTS §9) |
| **M** | 카운트 조건부 `asof_*` 피처 | ✅ **val 완료** (+7.6) — 배포 재굽기 대기 |
| N | 8~10월 블록 진단 | 대기 |
| O | 스윙맨 역할 피처 | 대기 |
