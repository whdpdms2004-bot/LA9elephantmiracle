# WORKFLOW_COMPARISONS 진행 현황 — 2026-08-27 마감

원본: `cowork/sj/WORKFLOW_COMPARISONS.md` §29.2 (본선 S1~S6.5 + 곁가지 8축).
이 문서는 **어디까지 갔고 무엇이 남았는가**만 적는다. 실험 근거는 `results.csv`,
모델 구성은 `models/<name>.md` 에 있다.

---

## 0. 한 줄 요약

**본선은 종점까지 갔고 way 분해는 기각됐다.** 이번 캠페인의 실질 산출물은 본선이
아니라 곁가지에서 나왔다 — `id_freq`(축1) 와 CB 격자 재탐색(조건부 조항).

    Public  1072.865 (챔피언)  ->  1080.425   +7.56

**마지막 +3.7 은 전부 CB 격자(`depth 5->6`)에서 나왔다.** 같이 넣었던 팀 가중
변경은 실측 0.026 으로 무효였다 (두 제출본을 나눠 올려 분리 확인).

---

## 1. 본선 (§29.2 표)

| | 단계 | 상태 | 결과 |
|---|---|---|---|
| **S1** | 168 1WAY 기준선 + 정직한 OOF | ✅ 완료 | 기준선 확정. `S1_base` = cb+ft+mlp, val2024 885.3 |
| **S2** | 1WAY 실패성분 분해 (R/F) | ✅ 완료 | **R 은 포화, F 가 전장.** 멤버 간 BSS 폭 R 1.9 / F 68. R 이 행의 88.2% |
| **S3** | 3WAY 타깃별 최적화 | ✅ 완료 | **게이트 실패 → way 분해 폐쇄.** 3WAY 가 1WAY 를 못 넘었다 |
| **S4** | 사건확률 합집합·보정 | ✅ S3 에 포함 | way 가 닫혀 독립 단계로 돌릴 대상이 없다 |
| **S5** | way 계열평균 identity | ✅ 완료 | **기각.** 자체적합 +9.6 이 정직 전이로는 **+1.2** |
| **S6** | way 표현 concat fusion | ✅ 완료 | **기각.** +0.1 |
| **S6.5** | 증류 D0~D4 (축4) | ✅ 완료 | **전부 음수.** D4(상관 페널티)만 ρ 를 벗어났으나 신호가 남지 않았다 (단독 −11.1) |

**S3 게이트 실패 후 규정대로** ("본선이 무너지면 남은 배치를 전부 곁가지 심화에")
자원을 곁가지로 돌렸다. 그 판단이 맞았다 — 이번 캠페인의 모든 이득이 곁가지에서 나왔다.

---

## 2. 곁가지 8축

| 축 | 내용 | 상태 | 결과 |
|---|---|---|---|
| **1** | 전처리 원자 8종 | ✅ 완료 | ★ **`id_freq` 채택.** cb 단독 val2024 881.8→**902.8** (+21.0) / val2022 +10.8. **이번 캠페인 최대 발견** |
| 2 | prep 변환 축 | ⬜ **미실행** | 대상이 `mlp` 인데 블렌드에서 `mlp` 를 빼는 게 이득(−7.2)이라 대상 소멸 |
| 3 | FT 하이퍼 | ◐ 거의 완료 | 이득 없음. `dtok32` 한 arm 은 2h43m 정체로 중단 — `layer2` 가 같은 방향을 커버해 결론 불변 |
| 4 | 증류 D0~D4 | ✅ 완료 | 전부 음수 (S6.5 와 동일) |
| 5 | 시드 수 3/5/8 | ✅ 완료 | **3시드로 충분.** Δ(8−3) 이 val2024 −0.6 · val2022 −0.9 |
| **6** | 279 ∪ 168 합집합 | ⬜ **미실행** | 계획서가 "기대부호 마이너스" + "정렬 예산 초과 시 버린다" 고 단서. sj 279 행렬 재생성 필요 |
| **7** | 모델 계열 (xgb/lgb/dart/et…) | ⚠️ **재검증됨 (08-28)** | 당시 결론 "단독 ~ ρ(cb) = +0.994" 는 **오염의 산물**. 원시로는 ρ 0.75~1.00 으로 퍼진다. 다만 **ρ 가 낮다고 좋은 것도 아니다** — ρ 최저 멤버가 가중 −0.014 |
| **8** | F/최근성 전문가 | ✅ 완료 (신설) | 다양성을 만드는 **유일한 레버는 다른 학습 데이터** (F행만/최근 시즌만) |

### 조건부 재탐색 — CB 격자 ✅ **오늘 완료**

§29.2 가 "입력 피처셋이 168 에서 **실제로 바뀌면**" 발동하도록 달아둔 조항.
`id_freq` 로 176 이 되며 발동했다. 9설정 × 3짝시드 × 2폴드 = 54판, **16.4분**.

**두 폴드가 반대 방향을 원한다** — val2024 는 깊고 약한 규제, val2022 는 강한 규제.
2022 는 `game_type` 구조 단절 **이전**이라(F 성공률 0.7087 vs 2024 의 0.4593)
다른 최적점을 갖는 것이 자연스럽다.

    채택   depth 5 -> 6, l2 10000 유지   val2024 +4.2 · val2022 +0.0 (관문 통과)
    탈락   d6_l3k                        val2024 +8.9 지만 val2022 -7.9

**깊이만 올리고 규제는 그대로**가 답이었다. val2024 만 봤으면 `d6_l3k` 를 골라
2022 를 −7.9 깎았을 것이다. 관문이 실제로 일했다.

---

## 2.5 08-28 캠페인 — 계획 A~G 완료

| # | 항목 | 결과 |
|---|---|---|
| A | CB 깊이 연장 `d7·d8` | `d6` 유지. 깊이와 두 폴드가 완전 역상관이고 `d6` 만 동시 만족 |
| B | end-to-end 역할 조정 | **채택** — 시드분산 상보 표본가중 (`sj_e2var`) |
| C | `mlp` → `cb_f_only` | **기각** — 원시 계기에서 val2022 −64.6 |
| **D** | [축2] prep 변환 | **★채택 · 최대 이득** — `std` 표현으로 mlp 부활 (`sj_stdmlp`) |
| E | F 부분군 라우팅 | 생존하나 소이득 (+0.8/+2.3), 미채택 |
| F | `ft` × 표현 | **기각** — 단독 +22 인데 결합 0 |
| G | [축6] 279 ∪ 168 | **닫음** — 저장소 내 직접 증거 |
| H | [축7] 재검증 (신설) | **방법론 정정** — ρ 0.994 선은 오염의 산물 |

**★ 가장 큰 소득은 계기 오염을 잡은 것이다.** `run_arm.py:307` · `run_family.py:343`
이 저장하는 예측은 `calib(p, yv)` 의 출력이고, 그 함수가 **평가 폴드 라벨로**
로짓을 재중심화하고 스케일을 고른다. 같은 `d6_l10k` 가 저장 904.1 / 원시 851.3.

그 결과 두 결론이 뒤집혔다 — `cb_f_only`("3.42σ 채택안")와 축7의 ρ 선.

## 3. 남은 것 (우선순위 순)

| # | 항목 | 예상 시간 | 왜 남았나 |
|---|---|---|---|
| 1 | 구간별 결합가중 (볼카운트 · R/F) | ~1시간 | λ 수축으로 양방향 생존하나 이득이 작다 (+0.8~1.5). 결합부가 `asof_pitcher_n` 에 하드코딩돼 스크립트 수정 필요 |
| 2 | **네 번째 표현 축 찾기** | ? | 3멤버가 표현 축을 한 번씩 덮어 포화다. 새 이득은 **아직 아무도 담지 않은 표현**에서만 온다 — 무엇이 있는지가 열린 질문 |
| 3 | ye 의 OOF 확보 | 팀 요청 | `cowork/ye/` 는 완전히 독립된 파이프라인인데 산출물이 저장소에 없다. 팀 최대 미확인 자산 |

---

## 4. 방법론 — 이번 캠페인이 남긴 것

결과보다 **판정 계기**가 더 값나갔다. 아래는 재사용할 것.

1. **자체적합은 반드시 부풀린다.** way 계열평균 자체적합 +9.6 → 정직 +1.2.
   적합 fold 와 평가 fold 를 반드시 분리한다.
2. **적합 fold 는 2022·R 이어야 한다.** fold 2023 은 두 부분군에서 발산하고
   (`cb +2.854 / ft −2.328`) 2024 평가에서 248.4 를 낸다 (fit(2022·R) 은 877.8).
3. **오차 상관은 못 쓴다.** 예측 분산이 라벨 분산의 0.86% 라 모든 모델의 오차 ρ ≈ 1
   (cb·ft 예측 ρ 0.9313 vs 오차 ρ 0.9994). 같은 이유로 `huber(α=0.9)` 가
   squared error 와 **비트 단위로 동일**하게 나왔다.
4. **`단독 ~ ρ(cb)` 선을 벗어나는 길은 다른 학습 데이터뿐이다** (r=+0.994, 21멤버).
5. **결합층은 합=1 로 묶는다.** 자유 적합은 같은 분할에서 **−24.8**.
6. **하네스가 리더보드로 검증됐다** — 제출3(합 1.143)의 Δval −6.1 예측 → 실제 −6.7.
7. **비교 대상을 틀리면 결론이 뒤집힌다.** `yn` 은 내 스택과 ρ 0.8949 로 탈상관처럼
   보이지만 실제 결합 상대인 `sj3way` 와는 **0.9572** 다.

---

## 5. 관측된 전이율 (val → Public)

| 변경 종류 | val Δ | Public Δ | 전이율 |
|---|---:|---:|---:|
| 피처 추가 (`id_freq`) | +21.0 (cb 단독) | +1.33 | **0.06** |
| cw 내부 가중 재적합 | — | +2.53 | — |
| 팀 가중 — 합 1.143 (분산 팽창) | −6.1 | −6.7 | **1.1** |
| **CB 하이퍼 (`depth 5→6`)** | **+4.2** (cw 모듈) | **+3.68** | **0.88** |
| 팀 가중 — 합=1 안에서 `w_cw` 0.41→0.64 | +2.2 | −0.03 | **0.0** |

**세 종류가 전혀 다르게 전이된다.**

1. **하이퍼파라미터 0.88** — 가장 잘 전이된다. 같은 피처·같은 구조에서 용량만
   조정하는 것이라 시즌이 바뀌어도 방향이 유지된다.
2. **피처 0.06** — val2024 에서 +21 이 Public 에서 +1.33 이 됐다. `id_freq` 의
   실체가 "학습에 없던 투수"인데 그 비율이 폴드마다 다르기 때문으로 보인다.
3. **결합 가중 — 부호에 따라 갈린다.** 해로운 변화(합 이탈)는 1.1 로 그대로
   전이되고, 이로운 미세조정(합=1 안)은 0.0 이다. 즉 **결합층에서 잃을 수는
   있어도 벌 수는 없다.**

**따라서 남은 시간은 하이퍼파라미터와 구조에 쓴다.** 결합 가중 미세조정은
기대값이 0 으로 실측됐다.
