# <name>

> 규칙 2 서식. `models/<name>.md` 로 복사해 채운다. 이름은 zip·폴더와 글자까지 같게.
> 빈 칸을 남기지 않는다. 모르는 값은 "미측정"이라고 쓴다 — 공란은 측정한 척으로 읽힌다.

| 항목 | 값 |
|---|---|
| 제출일 | 2026-08-__ |
| zip | `models/<name>.zip` |
| 학습 스크립트 | `models/<name>/` |
| val 예측 | `val/<name>_2022.csv`, `val/<name>_2024.csv` |
| 기준 모델 | `<baseline name>` |
| 판정 | 채택 / 기각 (규칙 1: 2024 상승 **and** 2022 비하락) |

---

## 1. 성능

| 시즌 | all | R | F | early(3-5) | mid(6-7) | late(8-10) | pred_mean | true_mean |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 2024 (주) | | | | | | | | |
| 2022 (비하락 조건) | | | | | | | | |

기준 모델 대비 델타:

| 시즌 | Δall | 하락? |
|---|---:|---|
| 2024 | | — |
| 2022 | | 예/아니오 |

Public: (제출 후 기입) · f24→Public 전이율 참고 0.67

## 2. 모델 구성

- **타깃/구조**: (1WAY 직접 success / 3WAY middle·reverse·outside 결합 / 5WAY / NODE-MRO 등)
- **결합 방식**: (way→success 매핑, 가중치, 온도, 적합 fold 와 동결 fold)
- **베이스 학습기**: (CatBoost / LightGBM / MLP / FT-Transformer, 개수와 시드)
- **하이퍼파라미터**: (핵심만. 전문은 `models/<name>/params.json` 참조)
- **피처 세트**: (대회 원본 + 추가 transform 목록. 각 transform 의 as-of 기준 명시)
- **학습 구간**: (시즌 범위. val 시즌이 빠졌는지 반드시 확인)
- **후처리**: (클리핑, 캘리브레이션, 기저율 보정 — 했으면 어떤 fold 에서 적합했는지)

## 3. 앙상블 관점

| 상대 모델 | 확률 상관 | 오차 상관 | 최적 가중 | 결합 2024 all |
|---|---:|---:|---:|---:|
| | | | | |

- 이 모델이 **남과 다르게 맞히는 구간**: (부분군·월 블록 단위로)
- 결합에서 빼야 하는 조건:

## 4. 규정 점검 (cowork/RULES.md 0절)

- [ ] `script.py` 는 추론 전용 — 학습 코드 없음
- [ ] test 행 간 정보 사용 없음 (누적·rolling·집계·분포 보정 전부 없음)
- [ ] 외부 데이터·외부 API·추론 시 인터넷 접근 없음
- [ ] 투구 이후 시점 정보 없음
- [ ] `requirements.txt` 만으로 실행 재현됨

## 5. 재현 절차

```bash
# models/<name>/ 안에서
python train.py --seed 0        # 예시. 실제 명령 그대로 적는다
```

소요: (GPU/CPU, 시간) · 데이터 as-of: (사용한 lookup/manifest 파일)

## 6. 남은 것

-
