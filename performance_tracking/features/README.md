# `features/` — 각 제출본이 쓴 피처 목록

모델 카드에는 "57피처" "71피처" 같은 **숫자만** 있었다. 그러면 누가 무엇을 쓰고
무엇을 안 쓰는지 비교할 수 없다. 여기에 **이름 목록**을 둔다.

    python tools/extract_features.py     # 전부 다시 뽑는다

## 목록

| 파일 | 개수 | 완전성 | 출처 |
|---|---:|---|---|
| `cw_v17_base.txt` | 168 | 완전 | `cowork/sj/sj_final/work/meta.json` |
| `sj_stdmlp.txt` | 176 | 완전 | 위 168 + `atoms.id_freq` 8 |
| `sj3way.txt` | 272 | 완전 | 챔피언 zip 의 `model/sj/model/base_metadata.json` |
| `ye_hand.txt` | 60 | 완전 | `models/sj_stdmlp/ye_repro.py` (노트북 셀3 이식) |
| `hw_v12.txt` | 57 | 완전 | `models/hw_v12/build_val2024_pred_v12.py` 의 구성 규칙 |
| `yn_fa10c.txt` | 50 | **★불완전** | `models/yn_fa10c.zip` 의 `model/meta.json` |

## ★ `yn_fa10c` 는 부분만 복원된다

`SUBMISSION_LOG.md` 는 **71피처(68 파생 + 1군/2군 오염보정 3)** 라고 적었는데,
`meta.json` 에 있는 것은 **원시 입력 47 + 신규 3** 뿐이다.
**파생 68개의 이름이 저장소에 없다** — 추론 스크립트가 만들어 쓰고 버린다.
학습 코드도 없어서(`models/yn_fa10c/MISSING.md`) 복원할 방법이 없다.

**yn 에게 요청할 것**: 학습 파이프라인 전체, 또는 최소한 최종 피처 이름 목록.

## 겹침 (교집합 / 합집합, val2024 기준)

|  | cw168 | sj176 | hw57 | ye60 | yn50 | sj3way272 |
|---|---|---|---|---|---|---|
| **cw_v17_base** | 167/167 | 167/175 | 45/179 | 20/207 | 44/173 | 45/394 |
| **sj_stdmlp** | 167/175 | 175/175 | 45/187 | 20/215 | 44/181 | 45/402 |
| **hw_v12** | 45/179 | 45/187 | 57/57 | 22/95 | 47/60 | 49/280 |
| **ye_hand** | 20/207 | 20/215 | 22/95 | 60/60 | 22/88 | 22/310 |
| **yn_fa10c** | 44/173 | 44/181 | 47/60 | 22/88 | 50/50 | 47/275 |
| **sj3way** | 45/394 | 45/402 | 49/280 | 22/310 | 47/275 | 272/272 |

**`ye` 가 겹침이 가장 적다** (cw/sj 와 20개만 공유). 그래서 예측 상관도 팀 최저
(ρ(cw) 0.7952) 인데, **단독 성능이 574.7 로 낮아 결합 가중은 0** 이다.
"다른 피처면서 단독 신호도 있어야 한다" 는 이번 캠페인의 결론이 여기서도 보인다.

**`cw_v17_base` 는 168개 중 고유 167** — `cnt_31_x_tend` 가 중복이다.

## 주의 — 이름이 같다고 같은 값이 아니다

`sj_stdmlp` 은 같은 176열을 **모델마다 다른 표현**으로 준다.

    cb   176열 원시값        (트리 — 순서만 씀)
    ft   168열 분위수 순위
    mlp  176열 로버스트 z 점수 + 결측마스크 (352 입력)

이 표현 차이가 blend 이득의 실체다 — 피처를 더하는 것만으로는 효과가 0 이었다.
